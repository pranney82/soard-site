---
/**
 * Footer.astro — Best-in-class nonprofit footer
 *
 * Structure:
 *   1. Newsletter CTA band (warm gray)
 *   2. Main footer: logo + mission, nav columns, social, donate CTA (dark)
 *   3. Trust signals bar: Candid, 501(c)(3), EIN (dark-deep)
 *   4. Legal bar: copyright, privacy, terms (dark-deep, bottom)
 *
 * Accessibility: All links have descriptive text, external links marked,
 *   social icons have aria-labels, skip-to-footer landmark
 * SEO: address in <address> tag, tel: and mailto: links, external rels
 * Performance: SVG social icons inline (no external requests)
 */
import site from '../../content/site/settings.json';
import { cfRaw, cfSrcset } from '../../utils/cf-image';

const FOOTER_LOGO_ID = 'brand-logo-dark-bg-tagline-display';
const year = new Date().getFullYear();
const { phone, email, address, social } = site;
const phoneDigits = phone.replace(/\D/g, '');
const fullAddress = `${address.street.replace(', Suite ', ', Ste ')}, ${address.city} ${address.state} ${address.zip}`;
const mapsUrl = `https://maps.google.com/?q=${encodeURIComponent(`${address.street}, ${address.city}, ${address.state} ${address.zip}`)}`;
---

<footer class="site-footer">

  <!-- ─── 1. NEWSLETTER BAND ─────────────────── -->
  <div class="footer-newsletter">
    <div class="footer-container footer-newsletter-inner">
      <div class="footer-newsletter-text">
        <h2><span class="highlight">Brighten your inbox</span> with a little Sunshine.</h2>
        <p>Room reveals, impact stories, and upcoming events, delivered monthly.</p>
      </div>
      <form class="newsletter-form" id="newsletter-form" action="/api/newsletter" method="POST">
        <div class="newsletter-fields">
          <input
            type="email"
            name="email"
            placeholder="Your email address"
            required
            autocomplete="email"
            aria-label="Email address for newsletter"
            class="newsletter-input"
            id="newsletter-email"
          />
          <button type="submit" class="newsletter-btn" id="newsletter-btn">
            <span class="btn-text">Subscribe</span>
            <span class="btn-sending" aria-hidden="true">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M12 2v4"/><path d="m16.2 7.8 2.9-2.9"/><path d="M18 12h4"/><path d="m16.2 16.2 2.9 2.9"/><path d="M12 18v4"/><path d="m4.9 19.1 2.9-2.9"/><path d="M2 12h4"/><path d="m4.9 4.9 2.9 2.9"/></svg>
            </span>
            <span class="btn-done" aria-hidden="true">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
            </span>
          </button>
        </div>
        <!-- Honeypot — hidden field that bots fill out, humans don't see -->
        <div aria-hidden="true" style="position:absolute;left:-9999px;top:-9999px;"><input type="text" name="website" tabindex="-1" autocomplete="off" /></div>
        <p class="newsletter-note" id="newsletter-note" role="status" aria-live="polite">No spam, ever. Unsubscribe anytime.</p>
      </form>
    </div>
  </div>

  <!-- ─── 2. MAIN FOOTER ─────────────────────── -->
  <div class="footer-main">
    <div class="footer-container footer-main-grid">

      <!-- Brand Block -->
      <div class="footer-brand">
        <a href="/" class="footer-logo" aria-label="Sunshine on a Ranney Day — Home">
          <img
              src={cfRaw(FOOTER_LOGO_ID, 'w=400,q=85')}
              srcset={cfSrcset(FOOTER_LOGO_ID, [200, 400])}
              sizes="200px"
              alt="Sunshine on a Ranney Day logo"
              width="200"
              height="200"
              loading="lazy"
            />
        </a>
        <p class="footer-mission">
          A 501(c)(3) nonprofit creating life-changing home makeovers for children with special needs in the greater Atlanta area.
        </p>

        <!-- Social Icons -->
        <div class="footer-social">
          {social.facebook && <a href={social.facebook} target="_blank" rel="noopener noreferrer" aria-label="Facebook" class="footer-social-link">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
          </a>}
          {social.instagram && <a href={social.instagram} target="_blank" rel="noopener noreferrer" aria-label="Instagram" class="footer-social-link">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z"/></svg>
          </a>}
          {social.youtube && <a href={social.youtube} target="_blank" rel="noopener noreferrer" aria-label="YouTube" class="footer-social-link">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M23.498 6.186a3.016 3.016 0 00-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 00.502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 002.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 002.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/></svg>
          </a>}
          {social.tiktok && <a href={social.tiktok} target="_blank" rel="noopener noreferrer" aria-label="TikTok" class="footer-social-link">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12.525.02c1.31-.02 2.61-.01 3.91-.02.08 1.53.63 3.09 1.75 4.17 1.12 1.11 2.7 1.62 4.24 1.79v4.03c-1.44-.05-2.89-.35-4.2-.97-.57-.26-1.1-.59-1.62-.93-.01 2.92.01 5.84-.02 8.75-.08 1.4-.54 2.79-1.35 3.94-1.31 1.92-3.58 3.17-5.91 3.21-1.43.08-2.86-.31-4.08-1.03-2.02-1.19-3.44-3.37-3.65-5.71-.02-.5-.03-1-.01-1.49.18-1.9 1.12-3.72 2.58-4.96 1.66-1.44 3.98-2.13 6.15-1.72.02 1.48-.04 2.96-.04 4.44-.99-.32-2.15-.23-3.02.37-.63.41-1.11 1.04-1.36 1.75-.21.51-.15 1.07-.14 1.61.24 1.64 1.82 3.02 3.5 2.87 1.12-.01 2.19-.66 2.77-1.61.19-.33.4-.67.41-1.06.1-1.79.06-3.57.07-5.36.01-4.03-.01-8.05.02-12.07z"/></svg>
          </a>}
        </div>

        <!-- Donate CTA -->
        <a href="/donate/" class="footer-donate-btn">
          Make a Donation
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="M8 2.5C6.5 1 4 1 2.75 2.75S2.5 6.5 8 11.5C13.5 6.5 13.75 4.5 13.25 2.75S9.5 1 8 2.5Z" fill="currentColor"/></svg>
        </a>
      </div>

      <!-- Nav Columns -->
      <nav class="footer-nav" aria-label="Footer navigation">
        <div class="footer-col">
          <h3 class="footer-heading">About Us</h3>
          <ul>
            <li><a href="/about/">Our Story</a></li>
            <li><a href="/about/leadership/">Leadership</a></li>
            <li><a href="/about/financials/">Financials</a></li>
            <li><a href="/publicity/">Press & Media</a></li>
            <li><a href="/branding/">Branding</a></li>
          </ul>
        </div>
        <div class="footer-col">
          <h3 class="footer-heading">Our Kids</h3>
          <ul>
            <li><a href="/kids/">Meet the Kids</a></li>
            <li><a href="/community/">Community Projects</a></li>
            <li><a href="/apply/">Apply for Makeover</a></li>
          </ul>
        </div>
        <div class="footer-col">
          <h3 class="footer-heading">Get Involved</h3>
          <ul>
            <li><a href="/events/">Events</a></li>
            <li><a href="/partners/">Partners</a></li>
            <li><a href="/advisory-board/">Advisory Board</a></li>
            <li><a href="/ways-to-give/">Ways to Give</a></li>
            <li><a href="/sunny-and-ranney/">Sunny & Ranney</a></li>
          </ul>
        </div>
        <div class="footer-col">
          <h3 class="footer-heading">Support</h3>
          <ul>
            <li><a href="/donate/">Donate</a></li>
            <li><a href="/resources/">Resources</a></li>
            <li><a href="/faq/">FAQs</a></li>
          </ul>
        </div>
      </nav>
    </div>
  </div>

  <!-- ─── 3. TRUST SIGNALS BAR ────────────────── -->
  <div class="footer-trust">
    <div class="footer-container footer-trust-inner">
      <div class="footer-trust-badges">
        <a href="https://www.charitynavigator.org/ein/454773997"
           target="_blank" rel="noopener noreferrer" class="trust-badge" aria-label="View our Charity Navigator profile">
          <img
            src="/charity-navigator-3-star.png"
            alt="Charity Navigator 3-Star Rating"
            width="64"
            height="64"
            loading="lazy"
            onerror="this.src='https://www.charitynavigator.org/images/unified_stars_dashboard_graphics/three_star.svg';this.onerror=()=>{this.parentElement.style.display='none'}"
          />
        </a>
        <a href="https://www.guidestar.org/profile/shared/2954b510-8c76-42e4-b589-7e826ca2cc47"
           target="_blank" rel="noopener noreferrer" class="trust-badge" aria-label="View our Candid Platinum Transparency profile">
          <img
            src="/candid-platinum-2026.svg"
            alt="Candid Platinum Transparency 2026"
            width="64"
            height="64"
            loading="lazy"
          />
        </a>
        <div class="trust-badge trust-badge--text">
          <span class="trust-badge-label">Registered 501(c)(3)</span>
          <span class="trust-badge-value">EIN: 45-4773997</span>
        </div>
        <div class="trust-badge trust-badge--text">
          <span class="trust-badge-label">Tax-Deductible</span>
          <span class="trust-badge-value">All donations</span>
        </div>
        <a href="/files/financials/ga-contractor-license.pdf" target="_blank" rel="noopener noreferrer" class="trust-badge trust-badge--text" aria-label="View Georgia Contractor License">
          <span class="trust-badge-label">Licensed Contractor</span>
          <span class="trust-badge-value">All projects permitted</span>
        </a>
      </div>

      <address class="footer-contact">
        <a href={mapsUrl} target="_blank" rel="noopener noreferrer" class="footer-contact-item">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 10c0 4.993-5.539 10.193-7.399 11.799a1 1 0 0 1-1.202 0C9.539 20.193 4 14.993 4 10a8 8 0 0 1 16 0"/><circle cx="12" cy="10" r="3"/></svg>
          {fullAddress}
        </a>
        <a href={`tel:+1${phoneDigits}`} class="footer-contact-item">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M13.832 16.568a1 1 0 0 0 1.213-.303l.355-.465A2 2 0 0 1 17 15h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2A18 18 0 0 1 2 4a2 2 0 0 1 2-2h3a2 2 0 0 1 2 2v3a2 2 0 0 1-.8 1.6l-.468.351a1 1 0 0 0-.292 1.233 14 14 0 0 0 6.392 6.384"/></svg>
          {phone}
        </a>
        <a href={`mailto:${email}`} class="footer-contact-item">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-8.991 5.727a2 2 0 0 1-2.009 0L2 7"/></svg>
          {email}
        </a>
      </address>
    </div>
  </div>

  <!-- ─── 4. LEGAL BAR ────────────────────────── -->
  <div class="footer-legal">
    <div class="footer-container footer-legal-inner">
      <p class="footer-copyright">
        &copy; {year} Sunshine on a Ranney Day. All rights reserved.
      </p>
      <p class="footer-built-by">
        Website built by Sunshine Team & Peter Ranney
      </p>
      <div class="footer-legal-links">
        <a href="/privacy-policy/">Privacy Policy</a>
        <a href="/terms/">Terms of Use</a>
        <a href="/admin/" class="footer-admin-link">Admin</a>
      </div>
    </div>
  </div>
  <script>
    document.addEventListener('astro:page-load', () => {
      const form = document.getElementById('newsletter-form');
      if (!form) return;
      const note = document.getElementById('newsletter-note');
      const btn = document.getElementById('newsletter-btn');
      const input = document.getElementById('newsletter-email');

      // ── No-JS fallback: read query params from form redirect ──
      const params = new URLSearchParams(window.location.search);
      if (params.has('subscribed')) {
        btn.classList.add('is-done');
        note.textContent = "You're in! Welcome to the Sunshine family.";
        note.classList.add('newsletter-note--success');
        // Clean URL without reloading
        history.replaceState(null, '', window.location.pathname + window.location.hash);
      } else if (params.has('newsletter_error')) {
        const err = params.get('newsletter_error');
        const messages = {
          invalid_email: 'Please enter a valid email address.',
          not_configured: 'Newsletter signup is temporarily unavailable. Please try again later.',
          rate_limited: 'Too many attempts — please wait a minute and try again.',
          server: 'Something went wrong. Please try again.',
        };
        note.textContent = messages[err] || messages.server;
        note.classList.add('newsletter-note--error');
        history.replaceState(null, '', window.location.pathname + window.location.hash);
      }

      function resetNote() {
        note.classList.remove('newsletter-note--success', 'newsletter-note--error');
      }

      // ── JS-enhanced submit (prevents full page reload) ──
      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        const email = input.value.trim();
        if (!email) return;

        // Honeypot check — bots fill hidden fields, humans don't
        const honeypot = form.querySelector('[name="website"]');
        const hp = honeypot ? honeypot.value : '';

        // Sending state
        resetNote();
        btn.classList.remove('is-done');
        btn.classList.add('is-sending');
        btn.disabled = true;
        input.disabled = true;

        try {
          const res = await fetch('/api/newsletter', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email, hp }),
          });
          // Body may not be JSON (e.g. Cloudflare error pages) — don't throw on parse
          const data = await res.json().catch(() => null);

          btn.classList.remove('is-sending');

          if (data && data.ok) {
            btn.classList.add('is-done');
            input.value = '';
            note.textContent = "You're in! Welcome to the Sunshine family.";
            note.classList.add('newsletter-note--success');
          } else {
            note.textContent = res.status === 429
              ? 'Too many attempts — please wait a minute and try again.'
              : (data && data.error) || 'Something went wrong. Please try again.';
            note.classList.add('newsletter-note--error');
            btn.disabled = false;
            input.disabled = false;
          }
        } catch {
          btn.classList.remove('is-sending');
          note.textContent = 'Something went wrong. Please try again.';
          note.classList.add('newsletter-note--error');
          btn.disabled = false;
          input.disabled = false;
        }
      });
    });
  </script>
</footer>

<style>
  /* ═══════════════════════════════════════════
     FOOTER — Best-in-class nonprofit
     ═══════════════════════════════════════════ */

  .site-footer { margin-top: auto; }

  .footer-container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding-inline: var(--page-padding);
  }

  /* ─── Newsletter Band ──────────────────── */
  .footer-newsletter {
    background: var(--c-warm-gray);
    padding: 72px 0;
  }
  .footer-newsletter-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 48px;
  }
  .footer-newsletter-text {
    max-width: 420px;
    flex-shrink: 0;
  }
  .footer-newsletter-text h2 {
    font-size: clamp(1.4rem, 2.5vw, 1.75rem);
    line-height: 1.3;
    margin-bottom: 8px;
  }
  .footer-newsletter-text p {
    font-size: 15px;
    color: var(--c-text-muted);
  }
  .newsletter-form {
    flex: 1;
    max-width: 480px;
  }
  .newsletter-fields {
    display: flex;
    gap: 8px;
  }
  .newsletter-input {
    flex: 1;
    min-height: 48px;
    padding: 14px 20px;
    border: 2px solid var(--c-border);
    border-radius: 60px;
    background: var(--c-white);
    font-family: var(--font-body);
    font-size: 15px;
    transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
  }
  .newsletter-input:focus {
    outline: none;
    border-color: var(--c-yellow);
    box-shadow: 0 0 0 3px rgba(255, 218, 36, 0.25);
  }
  .newsletter-input:focus-visible {
    outline: 3px solid var(--c-yellow);
    outline-offset: 2px;
    box-shadow: 0 0 0 5px rgba(255, 218, 36, 0.15);
  }
  .newsletter-btn {
    padding: 14px 28px;
    min-height: 48px;
    background: var(--c-yellow);
    color: var(--c-dark);
    font-family: var(--font-body);
    font-weight: 600;
    font-size: 15px;
    border: none;
    border-radius: 60px;
    cursor: pointer;
    white-space: nowrap;
    position: relative;
    z-index: 1;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .newsletter-btn:hover {
    background: var(--c-yellow-hover);
    transform: translateY(-2px);
    box-shadow: 0 4px 16px var(--c-yellow-glow);
  }
  .newsletter-note {
    font-size: 12px;
    color: var(--c-text-light);
    margin-top: 10px;
    padding-left: 20px;
    transition: color 0.3s ease;
  }
  .newsletter-note--success { color: #22c55e; }
  /* Error state stays on-brand (no red): dark weight + yellow highlight */
  .newsletter-note--error {
    color: var(--c-dark);
    font-weight: 600;
    background-image: linear-gradient(transparent 60%, rgba(255, 213, 0, 0.35) 60%);
    display: inline-block;
  }

  /* Button states */
  .newsletter-btn { position: relative; }
  .btn-sending, .btn-done { display: none; }
  .newsletter-btn.is-sending .btn-text { display: none; }
  .newsletter-btn.is-sending .btn-sending {
    display: inline-flex;
    animation: nl-spin 0.8s linear infinite;
  }
  .newsletter-btn.is-done .btn-text { display: none; }
  .newsletter-btn.is-done .btn-done { display: inline-flex; }
  .newsletter-btn.is-done {
    background: #22c55e;
    color: white;
    pointer-events: none;
  }
  @keyframes nl-spin { to { transform: rotate(360deg); } }

  @media (max-width: 768px) {
    .footer-newsletter { padding: 56px 0; }
    .footer-newsletter-inner { flex-direction: column; gap: 28px; text-align: center; }
    .footer-newsletter-text { max-width: 100%; }
    .newsletter-form { max-width: 100%; width: 100%; }
    .newsletter-fields { flex-direction: column; }
    .newsletter-note { text-align: center; padding-left: 0; }
  }

  /* ─── Main Footer ──────────────────────── */
  .footer-main {
    background: var(--c-dark-deep);
    color: rgba(255, 255, 255, 0.75);
    padding: 72px 0 56px;
  }
  .footer-main-grid {
    display: grid;
    grid-template-columns: 260px 1fr;
    gap: 64px;
  }

  /* Brand Block */
  .footer-brand { display: flex; flex-direction: column; gap: 16px; }
  .footer-logo {
    display: inline-block;
    min-height: 48px;
    transition: opacity var(--transition-fast);
    text-decoration: none;
  }
  .footer-logo:hover { opacity: 0.85; }
  .footer-logo img {
    display: block;
    width: 200px;
    height: auto;
    object-fit: contain;
  }
  .footer-logo-icon { width: 48px; height: 48px; flex-shrink: 0; }
  .footer-mission {
    font-size: 14px;
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.5);
    max-width: 260px;
  }

  /* Social Links */
  .footer-social {
    display: flex;
    gap: 12px;
  }
  .footer-social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.06);
    color: rgba(255, 255, 255, 0.6);
    transition: all 0.3s ease;
  }
  .footer-social-link:hover {
    background: var(--c-yellow);
    color: var(--c-dark);
    transform: translateY(-3px);
  }

  /* Donate Button */
  .footer-donate-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 14px 28px;
    width: 100%;
    max-width: 260px;
    margin-top: 4px;
    background: var(--c-yellow);
    color: var(--c-dark);
    font-family: var(--font-body);
    font-weight: 600;
    font-size: 15px;
    border-radius: 60px;
    text-decoration: none;
    transition: all 0.35s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .footer-donate-btn:hover {
    background: var(--c-yellow-hover);
    transform: translateY(-3px);
    box-shadow: 0 8px 24px var(--c-yellow-glow);
  }

  /* Nav Columns */
  .footer-nav {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 32px;
  }
  .footer-heading {
    font-family: var(--font-body);
    font-weight: 600;
    font-size: 14px;
    color: var(--c-white);
    margin-bottom: 16px;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
  .footer-col ul {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .footer-col a {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.55);
    text-decoration: none;
    transition: color var(--transition-fast), transform var(--transition-fast);
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding-block: 6px;
    min-height: 28px;
  }
  .footer-col a:hover {
    color: var(--c-yellow);
    transform: translateX(4px);
  }
  .external-icon {
    font-size: 11px;
    opacity: 0.5;
  }

  @media (max-width: 1024px) {
    .footer-main-grid {
      grid-template-columns: 1fr;
      gap: 48px;
    }
  }
  @media (max-width: 768px) {
    .footer-nav {
      grid-template-columns: 1fr 1fr;
      gap: 32px 24px;
    }
  }
  @media (max-width: 640px) {
    .footer-main { padding: 56px 0 40px; }
    .footer-brand { align-items: center; text-align: center; }
    .footer-logo img { width: 200px; }
    .footer-mission { max-width: 100%; }
    .footer-social { justify-content: center; }
    .footer-donate-btn { max-width: 100%; }
  }
  @media (max-width: 480px) {
    .footer-nav {
      grid-template-columns: 1fr 1fr;
      gap: 28px 20px;
    }
    .footer-heading { font-size: 13px; margin-bottom: 12px; }
    .footer-col a { font-size: 13px; }
  }

  /* ─── Trust Signals Bar ────────────────── */
  .footer-trust {
    background: var(--c-dark-deep);
    padding: 40px 0;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
  }
  .footer-trust-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 40px;
    flex-wrap: wrap;
  }
  .footer-trust-badges {
    display: flex;
    align-items: center;
    gap: 32px 36px;
    flex-wrap: wrap;
  }
  .trust-badge {
    display: flex;
    align-items: center;
    transition: opacity var(--transition-fast);
  }
  .trust-badge:hover { opacity: 0.8; }
  .trust-badge img {
    border-radius: 8px;
    opacity: 0.85;
  }
  .trust-badge--text {
    display: flex;
    flex-direction: column;
    gap: 2px;
    padding-left: 24px;
    border-left: 1px solid rgba(255, 255, 255, 0.08);
  }
  .trust-badge-label {
    font-size: 11px;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.5);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }
  .trust-badge-value {
    font-size: 14px;
    font-weight: 600;
    color: var(--c-yellow);
  }

  /* Contact */
  .footer-contact {
    font-style: normal;
    display: flex;
    gap: 24px;
    flex-wrap: wrap;
  }
  .footer-contact-item {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 13px;
    color: rgba(255, 255, 255, 0.55);
    text-decoration: none;
    transition: color var(--transition-fast);
    white-space: nowrap;
  }
  .footer-contact-item:hover { color: var(--c-yellow); }
  .footer-contact-item svg { flex-shrink: 0; opacity: 0.5; }

  @media (max-width: 768px) {
    .footer-trust-inner {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }
    .footer-trust-badges {
      justify-content: center;
      gap: 24px;
    }
    .trust-badge--text {
      border-left: none;
      padding-left: 0;
      align-items: center;
    }
    .footer-contact {
      flex-direction: column;
      align-items: center;
      gap: 12px;
    }
    .footer-contact-item { white-space: normal; text-align: center; }
  }

  /* ─── Legal Bar ────────────────────────── */
  .footer-legal {
    background: var(--c-dark-deep);
    padding: 20px 0 calc(20px + env(safe-area-inset-bottom, 0px));
    border-top: 1px solid rgba(255, 255, 255, 0.04);
  }
  .footer-legal-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    flex-wrap: wrap;
  }
  .footer-copyright {
    font-size: 12px;
    color: rgba(255, 255, 255, 0.5);
  }
  .footer-built-by {
    font-size: 12px;
    color: rgba(255, 255, 255, 0.65);
  }
  .footer-legal-links {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16px;
    font-size: 12px;
    color: rgba(255, 255, 255, 0.5);
  }
  .footer-legal-links a + a::before {
    content: '·';
    margin-right: 16px;
    color: rgba(255, 255, 255, 0.5);
  }
  .footer-legal-links a {
    color: rgba(255, 255, 255, 0.5);
    text-decoration: none;
    transition: color var(--transition-fast);
  }
  .footer-legal-links a:hover { color: var(--c-white); }
  .footer-admin-link { /* inherits parent link color */ }
  .footer-admin-link:hover { opacity: 1; }

  @media (max-width: 640px) {
    .footer-legal-inner {
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: 8px;
    }
    .footer-legal-links {
      justify-content: center;
      width: 100%;
    }
  }
</style>
