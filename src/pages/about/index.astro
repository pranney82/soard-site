---
/**
 * Our Story — Sunshine on a Ranney Day
 * Editorial long-form page: founding story, milestones, mascot, mission
 * 
 * PERF NOTES:
 * - Hero image: fetchpriority="high", eager loading, explicit dimensions
 * - All below-fold images: loading="lazy"
 * - data-reveal only on below-fold elements
 * - Team photos from CF Images
 */
import PageLayout from '../../layouts/PageLayout.astro';
import FAQSection from '../../components/ui/FAQSection.astro';
import { getFAQSchema } from '../../utils/schema';
import { getSiteStats } from '../../utils/stats';
import page from '../../content/site/our-story.json';
import faqData from '../../content/site/faq.json';

import { CF_BASE as CF, cfUrl, cfSrcset } from '../../utils/cf-image';

/* ── Auto-computed stats ── */
const computed = await getSiteStats();
const stats = {
  totalKids: computed.totalKids,
  totalRooms: computed.totalRooms,
  years: computed.years,
};
const founderPhotoId = page.images.founder;
const storePhotoId = page.images.store;
const graciePhotoId = page.images.gracie;
const trippPhotoId = page.images.tripp;
const milestones = page.milestones;

/* ── Breadcrumbs (BaseLayout emits WebPage + BreadcrumbList schema) ── */
const aboutDesc = `From one family's dream to ${stats.totalRooms}+ rooms transformed. See how Sunshine on a Ranney Day builds free bedrooms, bathrooms, and therapy spaces for kids with special needs in Atlanta.`;
const breadcrumbs = [
  { name: 'Home', url: 'https://sunshineonaranneyday.com/' },
  { name: 'Our Story', url: 'https://sunshineonaranneyday.com/about/' },
];
const aboutFaqs = faqData.items
  .filter(item => item.category === 'about' || item.category === 'rooms')
  .sort((a, b) => {
    if (a.category !== b.category) return a.category === 'about' ? -1 : 1;
    return (a.order || 0) - (b.order || 0);
  });
const jsonLd = getFAQSchema(aboutFaqs);
---

<PageLayout
  title="Our Story — A Family That Builds Dreams"
  description={aboutDesc}
  jsonLd={jsonLd}
  breadcrumbs={breadcrumbs}
  heroLabel="About"
  heroTitle={'Our <em class="highlight">Story</em>'}
  heroSubtitle="How it all started — and where we're headed"
>

  <!-- ══════ FOUNDERS — The Beginning ══════ -->
  <section class="founders" id="our-story" aria-labelledby="founders-heading">
    <div class="founders-inner">
      <div class="founders-photo">
        <img src={`${CF}/${founderPhotoId}/w=600,q=80`} srcset={`${CF}/${founderPhotoId}/w=400,q=80 400w, ${CF}/${founderPhotoId}/w=600,q=80 600w, ${CF}/${founderPhotoId}/w=800,q=80 800w`} sizes="(max-width:768px) calc(100vw - 48px), 460px" alt="Peter and Holly Ranney, founders of Sunshine on a Ranney Day" width="600" height="450" loading="eager" decoding="async" fetchpriority="high" />
      </div>
      <div class="founders-text" data-reveal>
        <div class="section-label">{page.founders.label}</div>
        <h2 id="founders-heading" set:html={page.founders.heading} />
        {page.founders.paragraphs.map((p: string) => <p set:html={p} />)}
      </div>
    </div>
  </section>

  <!-- ══════ TRIPP — The Turning Point ══════ -->
  <section class="turning-point" aria-labelledby="tripp-heading">
    <div class="turning-point-inner">
      <div class="turning-point-text" data-reveal>
        <div class="section-label">{page.turningPoint.label}</div>
        <h2 id="tripp-heading" set:html={page.turningPoint.heading} />
        {page.turningPoint.paragraphs.map((p: string) => <p set:html={p} />)}
      </div>
      <div class="turning-point-photo" data-reveal data-delay="1">
        <img src={`${CF}/${trippPhotoId}/w=600,q=80`} srcset={`${CF}/${trippPhotoId}/w=400,q=80 400w, ${CF}/${trippPhotoId}/w=600,q=80 600w, ${CF}/${trippPhotoId}/w=800,q=80 800w`} sizes="(max-width:768px) calc(100vw - 48px), 460px" alt="The Halstead family — Tripp's story inspired millions and helped expand SOARD's mission" width="600" height="807" loading="lazy" decoding="async" />
      </div>
    </div>
  </section>

  <!-- ══════ TIMELINE ══════ -->
  <section class="timeline-section" aria-labelledby="timeline-heading">
    <div class="timeline-header" data-reveal>
      <div class="section-label section-label--yellow">Milestones</div>
      <h2 id="timeline-heading">{stats.years} years of building <em>dreams</em>.</h2>
      <p class="timeline-subtitle">Every room starts with a story. Here's ours.</p>
    </div>
    <div class="timeline" id="timeline">
      <!-- Spine -->
      <div class="tl-track" aria-hidden="true">
        <div class="tl-track-fill"></div>
        <div class="tl-cursor"></div>
      </div>
      {milestones.map((m, i) => (
        <div class={`tl-item ${i % 2 === 0 ? 'tl-item--left' : 'tl-item--right'}`} data-tl-item>
          <div class="tl-dot" aria-hidden="true">
            <span class="tl-dot-ring"></span>
            <span class="tl-dot-core"></span>
          </div>
          <div class="tl-connector" aria-hidden="true"></div>
          <div class="tl-card">
            {m.image && (
              <div class="tl-image">
                <img
                  src={cfUrl(m.image, { w: 480, h: 280, fit: 'cover', gravity: 'face', q: 75 })}
                  srcset={cfSrcset(m.image, [320, 480, 640], { fit: 'cover', gravity: 'face', quality: 75, aspectRatio: 280/480 })}
                  sizes="(max-width:640px) calc(100vw - 72px), (max-width:960px) calc(100vw - 100px), 420px"
                  alt={m.title}
                  width="480"
                  height="280"
                  loading={i < 2 ? 'eager' : 'lazy'}
                  decoding="async"
                  class="lqip-img"
                />
              </div>
            )}
            <div class="tl-content">
              <span class="tl-year">{m.year}</span>
              <h3 class="tl-title">{m.title}</h3>
              <p class="tl-desc">{m.desc}</p>
              {m.detail && <p class="tl-detail">{m.detail}</p>}
            </div>
          </div>
        </div>
      ))}
      <!-- End beacon -->
      <div class="tl-cap" aria-hidden="true">
        <div class="tl-cap-year">{new Date().getFullYear()}+</div>
        <span class="tl-cap-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14"/><path d="m19 12-7 7-7-7"/></svg>
        </span>
        <div class="tl-cap-label">And counting</div>
      </div>
    </div>
  </section>

  <!-- ══════ IMPACT RIBBON ══════ -->
  <section class="ribbon" aria-label="Impact numbers">
    <div class="ribbon-inner">
      <div class="ribbon-stat" data-reveal>
        <span class="ribbon-num">{stats.totalRooms}+</span>
        <span class="ribbon-label">Room Makeovers</span>
      </div>
      <div class="ribbon-divider" role="presentation"></div>
      <div class="ribbon-stat" data-reveal data-delay="1">
        <span class="ribbon-num">{stats.totalKids}+</span>
        <span class="ribbon-label">Children Served</span>
      </div>
      <div class="ribbon-divider" role="presentation"></div>
      <div class="ribbon-stat" data-reveal data-delay="2">
        <span class="ribbon-num">{stats.years}</span>
        <span class="ribbon-label">Years of Impact</span>
      </div>
    </div>
  </section>

  <!-- ══════ GRACIE — The Mascot ══════ -->
  <section class="mascot" aria-labelledby="mascot-heading">
    <div class="mascot-inner">
      <div class="mascot-text" data-reveal>
        <div class="section-label">{page.mascot.label}</div>
        <h2 id="mascot-heading" set:html={page.mascot.heading} />
        {page.mascot.paragraphs.map((p: string) => <p set:html={p} />)}
        <p class="mascot-tagline" set:html={page.mascot.tagline} />
      </div>
      <div class="mascot-visual" data-reveal data-delay="1">
        <div class="mascot-photo">
          <img src={`${CF}/${graciePhotoId}/w=600,q=80`} srcset={`${CF}/${graciePhotoId}/w=400,q=80 400w, ${CF}/${graciePhotoId}/w=600,q=80 600w, ${CF}/${graciePhotoId}/w=800,q=80 800w`} sizes="(max-width:768px) calc(100vw - 48px), 460px" alt="Gracie the Pig — the real pot-bellied pig that inspired SOARD's mascot" width="600" height="400" loading="lazy" decoding="async" />
        </div>
      </div>
    </div>
  </section>

  <!-- ══════ SUNNY & RANNEY — The Store ══════ -->
  <section class="store-section" aria-labelledby="store-heading">
    <div class="store-inner">
      <div class="store-photo" data-reveal>
        <img src={`${CF}/${storePhotoId}/w=600,q=80`} srcset={`${CF}/${storePhotoId}/w=400,q=80 400w, ${CF}/${storePhotoId}/w=600,q=80 600w, ${CF}/${storePhotoId}/w=800,q=80 800w`} sizes="(max-width:768px) calc(100vw - 48px), 460px" alt="Sunny & Ranney, a nonprofit home furnishings store in Roswell, Georgia" width="600" height="450" loading="lazy" decoding="async" />
      </div>
      <div class="store-text" data-reveal data-delay="1">
        <div class="section-label">{page.store.label}</div>
        <h2 id="store-heading" set:html={page.store.heading} />
        {page.store.paragraphs.map((p: string) => <p set:html={p} />)}
        <a href="https://sunnyandranney.com" target="_blank" rel="noopener noreferrer" class="btn btn-dark">Shop Sunny & Ranney →</a>
      </div>
    </div>
  </section>

  <!-- ══════ WHERE WE ARE TODAY ══════ -->
  <section class="today-section" aria-labelledby="today-heading">
    <div class="today-inner" data-reveal>
      <div class="section-label">{page.today.label}</div>
      <h2 id="today-heading" set:html={page.today.heading} />
      <p class="today-body" set:html={page.today.body} />
      <div class="today-links">
        <a href="/about/leadership/" class="btn btn-dark">Meet Our Team →</a>
        <a href="/about/financials/" class="btn btn-outline-dark">View Financials →</a>
      </div>
    </div>
  </section>

  <!-- ══════ FINAL CTA ══════ -->
  <!-- ══════ FAQ ══════ -->
  <FAQSection
    heading='Common questions about <em class="highlight-dark">our mission</em>.'
    body="Learn more about who we are, what we build, and how it all works."
    ctaLabel="See All FAQs →"
    ctaHref="/faq/"
    items={aboutFaqs}
    accordionName="about-faq"
    id="faq"
  />

  <section class="story-finale">
    <div class="story-finale-inner">
      <h2 data-reveal set:html={page.finale.heading} />
      <p data-reveal data-delay="1" set:html={page.finale.body} />
      <div class="story-finale-ctas" data-reveal data-delay="2">
        <a href="/donate/" class="btn btn-dark btn-lg">Donate Today</a>
        <a href="/kids/" class="btn btn-outline-dark">Meet the Kids</a>
      </div>
      <p class="story-finale-note" data-reveal data-delay="3">501(c)(3) · EIN 45-4773997 · All donations tax-deductible · <a href="/publicity/" style="color: inherit; text-decoration: underline;">See our press coverage</a></p>
    </div>
  </section>

</PageLayout>

<style>
  /* ─── Page-specific overrides ──────────── */
  h2 em:not(.highlight) { font-style: italic; color: #8A7200; }

  /* ══════════════════════════════════════════
     FOUNDERS — Split layout
     ══════════════════════════════════════════ */
  .founders {
    background: var(--c-warm-gray);
    padding-block: var(--space-5xl) var(--space-4xl);
  }

  .founders-inner {
    max-width: var(--max-width);
    margin-inline: auto;
    padding-inline: var(--space-lg);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4xl);
    align-items: center;
  }

  .founders-photo {
    border-radius: var(--border-radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
  }

  .founders-photo img {
    width: 100%;
    height: auto;
    display: block;
  }

  .founders-text h2 {
    font-size: clamp(1.5rem, 3vw, 2.25rem);
    margin-bottom: var(--space-xl);
  }

  .founders-text p {
    font-size: 1.05rem;
    line-height: 1.75;
    color: var(--c-text);
    margin-bottom: var(--space-md);
  }

  .founders-text p:last-of-type {
    font-family: var(--font-display);
    font-style: italic;
    font-size: 1.1rem;
    color: var(--c-text-muted);
  }

  @media (max-width: 768px) {
    .founders { padding-block: var(--space-3xl); }
    .founders-inner {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }
  }

  /* ══════════════════════════════════════════
     TURNING POINT — Reversed split
     ══════════════════════════════════════════ */
  .turning-point {
    background: var(--c-warm-gray);
    padding-block: var(--space-4xl);
  }

  .turning-point-inner {
    max-width: var(--max-width);
    margin-inline: auto;
    padding-inline: var(--space-lg);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4xl);
    align-items: center;
  }

  .turning-point-text h2 {
    font-size: clamp(1.5rem, 3vw, 2.25rem);
    margin-bottom: var(--space-xl);
  }

  .turning-point-text p {
    font-size: 1.05rem;
    line-height: 1.75;
    color: var(--c-text);
    margin-bottom: var(--space-md);
  }

  .turning-point-photo {
    border-radius: var(--border-radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
  }

  .turning-point-photo img {
    width: 100%;
    height: auto;
    display: block;
  }

  @media (max-width: 768px) {
    .turning-point { padding-block: var(--space-3xl); }
    .turning-point-inner {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }
    .turning-point-text { order: 1; }
    .turning-point-photo { order: 2; }
  }

  /* ══════════════════════════════════════════
     TIMELINE — Scroll-driven cinematic
     ══════════════════════════════════════════ */
  .timeline-section {
    --tl-gutter: 60px;
    background: var(--c-dark);
    color: var(--c-white);
    padding-block: var(--space-5xl) 0;
    overflow: hidden;
    position: relative;
  }

  /* Subtle gradient overlay at top for seamless section transition */
  .timeline-section::before {
    content: '';
    position: absolute;
    inset: 0 0 auto;
    height: 120px;
    background: linear-gradient(180deg, rgba(45,46,51,0.6), transparent);
    pointer-events: none;
    z-index: 1;
  }

  .timeline-header {
    text-align: center;
    max-width: 640px;
    margin-inline: auto;
    margin-bottom: var(--space-5xl);
    padding-inline: var(--space-lg);
    position: relative;
    z-index: 2;
  }

  .timeline-header .section-label { justify-content: center; }

  .timeline-header h2 {
    color: var(--c-white);
    font-size: clamp(1.75rem, 4vw, 2.75rem);
  }

  .timeline-subtitle {
    margin-top: var(--space-md);
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.5);
    line-height: 1.6;
  }

  /* ── Track container ── */
  .timeline {
    position: relative;
    max-width: 1060px;
    margin-inline: auto;
    padding-inline: var(--space-lg);
    padding-bottom: var(--space-5xl);
  }

  /* ── Central spine ── */
  .tl-track {
    position: absolute;
    left: 50%;
    top: 0;
    bottom: 0;
    width: 2px;
    transform: translateX(-50%);
    background: rgba(255, 255, 255, 0.05);
    z-index: 0;
  }

  .tl-track-fill {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, var(--c-yellow) 0%, rgba(255, 218, 36, 0.6) 60%, rgba(255, 218, 36, 0.15) 100%);
    transform-origin: top;
  }

  .tl-ready .tl-track-fill {
    transform: scaleY(0);
    will-change: transform;
  }

  /* ── CSS scroll-driven fill — compositor thread, zero JS ──
   * .timeline gets a named view-timeline. Children reference it
   * so animation progress tracks the *timeline section* scrolling
   * through the viewport, not the individual element. */
  @supports (animation-timeline: view()) {
    .timeline.tl-ready {
      view-timeline-name: --tl;
      view-timeline-axis: block;
    }

    @keyframes tl-fill-scroll {
      from { transform: scaleY(0); }
      to   { transform: scaleY(1); }
    }

    .tl-ready .tl-track-fill {
      animation: tl-fill-scroll linear both;
      animation-timeline: --tl;
      animation-range: entry 0% exit 60%;
    }

    @keyframes tl-cursor-scroll {
      0%   { transform: translateY(0%); opacity: 0; }
      5%   { opacity: 1; }
      95%  { opacity: 1; }
      100% { transform: translateY(100%); opacity: 0; }
    }

    .tl-ready .tl-cursor {
      animation: tl-cursor-scroll linear both;
      animation-timeline: --tl;
      animation-range: entry 0% exit 60%;
      transition: none;
    }
  }

  /* ── Scroll cursor — rides the fill line ── */
  /* height: 100% makes translateY(%) parent-relative → compositable */
  .tl-cursor {
    position: absolute;
    left: 50%;
    top: 0;
    width: 0;
    height: 100%;
    transform: translateY(0%);
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: 3;
    pointer-events: none;
    will-change: transform, opacity;
  }

  .tl-cursor::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--c-yellow);
    transform: translate(-50%, -50%);
    box-shadow: 0 0 12px 4px rgba(255, 218, 36, 0.5), 0 0 40px 8px rgba(255, 218, 36, 0.15);
  }

  .tl-cursor.is-visible {
    opacity: 1;
  }

  /* ── Each milestone item ── */
  .tl-item {
    position: relative;
    display: grid;
    grid-template-columns: 1fr var(--tl-gutter) 1fr;
    gap: 0;
    align-items: start;
    margin-bottom: 72px;
    z-index: 1;
  }

  .tl-item:last-of-type { margin-bottom: 0; }

  /* Alternating sides */
  .tl-item--left .tl-card      { grid-column: 1; grid-row: 1; justify-self: end; }
  .tl-item--left .tl-dot       { grid-column: 2; grid-row: 1; }
  .tl-item--left .tl-connector { grid-column: 2; grid-row: 1; }
  .tl-item--right .tl-card     { grid-column: 3; grid-row: 1; justify-self: start; }
  .tl-item--right .tl-dot      { grid-column: 2; grid-row: 1; }
  .tl-item--right .tl-connector{ grid-column: 2; grid-row: 1; }

  /* ── Dot ── */
  .tl-dot {
    display: flex;
    align-items: start;
    justify-content: center;
    padding-top: 28px;
    position: relative;
    z-index: 2;
  }

  .tl-dot-ring {
    position: absolute;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    border: 2px solid rgba(255, 218, 36, 0.15);
    top: 20px;
    left: 50%;
    transform: translateX(-50%) scale(1);
    opacity: 1;
    transition: all 0.7s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .tl-ready .tl-dot-ring {
    transform: translateX(-50%) scale(0.4);
    opacity: 0;
  }

  .tl-dot-core {
    width: 14px;
    height: 14px;
    border-radius: 50%;
    background: var(--c-yellow);
    border: 2px solid var(--c-yellow);
    transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
    z-index: 1;
  }

  .tl-ready .tl-dot-core {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 255, 255, 0.12);
  }

  /* Active dot */
  .tl-item.is-active .tl-dot-core {
    background: var(--c-yellow);
    border-color: var(--c-yellow);
    box-shadow: 0 0 20px rgba(255, 218, 36, 0.6), 0 0 60px rgba(255, 218, 36, 0.15);
    transform: scale(1.1);
  }

  .tl-item.is-active .tl-dot-ring {
    opacity: 1;
    transform: translateX(-50%) scale(1);
    border-color: rgba(255, 218, 36, 0.3);
    animation: tl-ring-pulse 3s ease-in-out infinite;
  }

  @keyframes tl-ring-pulse {
    0%, 100% { transform: translateX(-50%) scale(1); opacity: 0.5; }
    50%      { transform: translateX(-50%) scale(1.6); opacity: 0; }
  }

  /* ── Connector ── */
  .tl-connector {
    position: absolute;
    top: 35px;
    height: 2px;
    background: rgba(255, 218, 36, 0.25);
    transition: background 0.5s ease, width 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    width: 30px;
  }

  .tl-ready .tl-connector {
    background: rgba(255, 218, 36, 0.12);
    width: 0;
  }

  .tl-item--left .tl-connector  { right: 50%; transform-origin: right; }
  .tl-item--right .tl-connector { left: 50%; transform-origin: left; }

  .tl-item.is-active .tl-connector {
    background: rgba(255, 218, 36, 0.25);
    width: 30px;
  }

  /* ── Card ── */
  .tl-card {
    max-width: 440px;
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border-radius: 16px;
    border: 1px solid rgba(255, 255, 255, 0.06);
    overflow: hidden;
    transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1),
                transform 0.8s cubic-bezier(0.16, 1, 0.3, 1),
                border-color 0.35s ease,
                box-shadow 0.35s ease,
                background 0.35s ease;
  }

  /* Only hide cards once JS has confirmed it can reveal them */
  .tl-ready .tl-card {
    opacity: 0;
  }
  .tl-ready .tl-item--left .tl-card  { transform: translateY(40px) translateX(30px); }
  .tl-ready .tl-item--right .tl-card { transform: translateY(40px) translateX(-30px); }

  .tl-item.is-active .tl-card {
    opacity: 1;
    transform: translateY(0) translateX(0);
  }

  /* Hover — lift + border glow + shadow */
  .tl-card:hover {
    border-color: rgba(255, 218, 36, 0.25);
    background: rgba(255, 255, 255, 0.06);
    box-shadow:
      0 12px 40px rgba(0, 0, 0, 0.4),
      0 0 0 1px rgba(255, 218, 36, 0.1),
      inset 0 1px 0 rgba(255, 255, 255, 0.05);
    transform: translateY(-6px) !important;
  }

  /* ── Card image ── */
  .tl-image {
    position: relative;
    overflow: hidden;
    aspect-ratio: 16 / 9;
  }

  /* Gradient overlay on image for text readability / polish */
  .tl-image::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, transparent 40%, rgba(45, 46, 51, 0.5) 100%);
    pointer-events: none;
  }

  .tl-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .tl-card:hover .tl-image img {
    transform: scale(1.06);
  }

  /* ── Card content ── */
  .tl-content {
    padding: 22px 26px 26px;
  }

  .tl-year {
    display: inline-block;
    font-family: var(--font-display);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    color: var(--c-yellow);
    background: rgba(255, 218, 36, 0.1);
    padding: 5px 14px;
    border-radius: 100px;
    margin-bottom: 14px;
  }

  .tl-title {
    font-family: var(--font-display);
    font-size: 1.2rem;
    font-weight: 700;
    color: var(--c-white);
    margin-bottom: 8px;
    line-height: 1.3;
  }

  .tl-desc {
    font-size: 0.93rem;
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.55);
  }

  .tl-detail {
    margin-top: 12px;
    font-family: var(--font-display);
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--c-yellow);
    letter-spacing: 0.02em;
    opacity: 0.85;
  }

  /* ── End cap ── */
  .tl-cap {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    padding-top: var(--space-3xl);
    position: relative;
    z-index: 2;
    transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .tl-ready .tl-cap {
    opacity: 0;
    transform: translateY(20px);
  }

  .tl-cap.is-active {
    opacity: 1;
    transform: translateY(0);
  }

  .tl-cap-year {
    font-family: var(--font-display);
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--c-yellow);
    letter-spacing: 0.05em;
  }

  .tl-cap-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: var(--c-yellow);
    color: var(--c-dark);
    box-shadow: 0 0 28px rgba(255, 218, 36, 0.35), 0 4px 20px rgba(0, 0, 0, 0.25);
  }

  .tl-cap.is-active .tl-cap-icon {
    animation: tl-cap-float 3s ease-in-out infinite;
  }

  @keyframes tl-cap-float {
    0%, 100% { transform: translateY(0); }
    50%      { transform: translateY(-8px); }
  }

  .tl-cap-label {
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.4);
  }

  /* ── Ambient glow ── */
  .timeline::after {
    content: '';
    position: absolute;
    left: 50%;
    top: 5%;
    bottom: 5%;
    width: 300px;
    transform: translateX(-50%);
    background: radial-gradient(ellipse at center, rgba(255, 218, 36, 0.035) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
  }

  /* ══════ TIMELINE — Tablet (≤960px) ══════ */
  @media (max-width: 960px) {
    .timeline-section { --tl-gutter: 44px; }

    .timeline {
      padding-left: 0;
      padding-right: var(--space-lg);
      margin-left: var(--space-lg);
    }

    .tl-track {
      left: 0;
      transform: none;
    }

    .tl-cursor { left: 0; }

    .timeline::after {
      left: 0;
      width: 160px;
      transform: none;
    }

    .tl-item {
      grid-template-columns: var(--tl-gutter) 1fr;
      margin-bottom: 56px;
    }

    .tl-item--left .tl-card,
    .tl-item--right .tl-card {
      grid-column: 2;
      grid-row: 1;
      justify-self: start;
      max-width: 100%;
    }

    .tl-item--left .tl-dot,
    .tl-item--right .tl-dot {
      grid-column: 1;
      grid-row: 1;
    }

    .tl-item--left .tl-connector,
    .tl-item--right .tl-connector { display: none; }

    .tl-item--left .tl-card,
    .tl-item--right .tl-card {
      transform: translateY(28px) translateX(-16px);
    }

    .tl-item.is-active .tl-card {
      transform: translateY(0) translateX(0);
    }
  }

  /* ══════ TIMELINE — Mobile (≤640px) ══════ */
  @media (max-width: 640px) {
    .timeline-section {
      --tl-gutter: 36px;
      padding-block: var(--space-3xl) 0;
    }

    .timeline-header { margin-bottom: var(--space-3xl); }

    .timeline {
      margin-left: var(--space-md);
      padding-right: var(--space-md);
      padding-bottom: var(--space-3xl);
    }

    .tl-item {
      grid-template-columns: var(--tl-gutter) 1fr;
      margin-bottom: 36px;
    }

    .tl-dot { padding-top: 20px; }

    .tl-dot-core { width: 10px; height: 10px; }

    .tl-dot-ring { width: 24px; height: 24px; top: 15px; }

    .tl-content { padding: 16px 18px 20px; }

    .tl-year { font-size: 0.7rem; padding: 4px 10px; margin-bottom: 10px; }

    .tl-title { font-size: 1rem; margin-bottom: 6px; }

    .tl-desc { font-size: 0.88rem; line-height: 1.65; }

    .tl-detail { font-size: 0.8rem; margin-top: 10px; }

    .tl-card {
      border-radius: 12px;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
      background: rgba(255, 255, 255, 0.05);
    }

    .tl-item {
      contain-intrinsic-size: auto 320px;
    }

    .tl-image {
      aspect-ratio: 16 / 10;
    }

    .tl-cap { padding-top: var(--space-2xl); }

    .tl-cap-icon { width: 40px; height: 40px; }

    .tl-cap-icon svg { width: 18px; height: 18px; }
  }

  /* ══════════════════════════════════════════
     IMPACT RIBBON
     ══════════════════════════════════════════ */
  .ribbon {
    background: var(--c-yellow);
    padding-block: var(--space-2xl);
  }

  .ribbon-inner {
    max-width: var(--max-width);
    margin-inline: auto;
    padding-inline: var(--space-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2xl);
    flex-wrap: wrap;
  }

  .ribbon-stat { text-align: center; }

  .ribbon-num {
    display: block;
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 700;
    color: var(--c-dark);
    line-height: 1;
  }

  .ribbon-pct { font-size: 0.6em; }

  .ribbon-label {
    display: block;
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: rgba(45, 46, 51, 0.85);
    margin-top: 6px;
  }

  .ribbon-divider {
    width: 1px;
    height: 48px;
    background: rgba(45, 46, 51, 0.2);
  }

  @media (max-width: 640px) {
    .ribbon-inner { gap: var(--space-lg); }
    .ribbon-divider { display: none; }
    .ribbon-stat { flex: 1 1 40%; }
  }

  /* ══════════════════════════════════════════
     MASCOT — Gracie
     ══════════════════════════════════════════ */
  .mascot {
    padding-block: var(--space-5xl);
  }

  .mascot-inner {
    max-width: var(--max-width);
    margin-inline: auto;
    padding-inline: var(--space-lg);
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: var(--space-4xl);
    align-items: center;
  }

  .mascot-text h2 {
    font-size: clamp(1.5rem, 3vw, 2.25rem);
    margin-bottom: var(--space-xl);
  }

  .mascot-text p {
    font-size: 1.05rem;
    line-height: 1.75;
    color: var(--c-text);
    margin-bottom: var(--space-md);
  }

  .mascot-tagline {
    font-family: var(--font-display);
    font-style: italic;
    font-size: 1.1rem !important;
    color: var(--c-text-muted) !important;
    margin-top: var(--space-lg) !important;
  }

  .mascot-visual {
    display: flex;
    justify-content: center;
  }

  .mascot-photo {
    border-radius: var(--border-radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
    transition: transform var(--transition-slow);
  }

  .mascot-photo:hover {
    transform: scale(1.02);
  }

  .mascot-photo img {
    width: 100%;
    height: auto;
    display: block;
  }

  @media (max-width: 768px) {
    .mascot { padding-block: var(--space-3xl); }
    .mascot-inner {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }
    .mascot-visual { order: -1; }
  }

  /* ══════════════════════════════════════════
     STORE — Sunny & Ranney
     ══════════════════════════════════════════ */
  .store-section {
    background: var(--c-warm-gray);
    padding-block: var(--space-4xl);
  }

  .store-inner {
    max-width: var(--max-width);
    margin-inline: auto;
    padding-inline: var(--space-lg);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4xl);
    align-items: center;
  }

  .store-photo {
    border-radius: var(--border-radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
  }

  .store-photo img {
    width: 100%;
    height: auto;
    display: block;
  }

  .store-text h2 {
    font-size: clamp(1.5rem, 3vw, 2.25rem);
    margin-bottom: var(--space-xl);
  }

  .store-text p {
    font-size: 1.05rem;
    line-height: 1.75;
    color: var(--c-text);
    margin-bottom: var(--space-md);
  }

  .store-text a:not(.btn) {
    color: var(--c-accent);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .store-text .btn { margin-top: var(--space-lg); }

  @media (max-width: 768px) {
    .store-section { padding-block: var(--space-3xl); }
    .store-inner {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }
  }

  /* ══════════════════════════════════════════
     TODAY — Where We Are
     ══════════════════════════════════════════ */
  .today-section {
    padding-block: var(--space-5xl);
    text-align: center;
  }

  .today-inner {
    max-width: 720px;
    margin-inline: auto;
    padding-inline: var(--space-lg);
  }

  .today-inner .section-label {
    justify-content: center;
  }

  .today-inner h2 {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    margin-bottom: var(--space-xl);
  }

  .today-body {
    font-size: 1.1rem;
    line-height: 1.8;
    color: var(--c-text);
  }

  .today-links {
    margin-top: var(--space-2xl);
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 768px) {
    .today-section { padding-block: var(--space-3xl); }
  }

  /* ══════════════════════════════════════════
     FINALE CTA
     ══════════════════════════════════════════ */
  .story-finale {
    background: var(--c-yellow);
    padding-block: var(--space-5xl);
    text-align: center;
  }

  .story-finale-inner {
    max-width: 700px;
    margin-inline: auto;
    padding-inline: var(--space-lg);
  }

  .story-finale h2 {
    font-size: clamp(1.75rem, 4vw, 2.75rem);
    color: var(--c-dark);
  }

  .story-finale h2 em {
    font-style: italic;
  }

  .story-finale > p,
  .story-finale-inner > p {
    font-size: 1.15rem;
    color: rgba(45, 46, 51, 0.8);
    margin-top: var(--space-md);
    line-height: 1.6;
  }

  .story-finale-ctas {
    margin-top: var(--space-2xl);
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    flex-wrap: wrap;
  }

  .story-finale-note {
    margin-top: var(--space-2xl);
    font-size: 0.85rem;
    color: rgba(45, 46, 51, 0.5);
    letter-spacing: 0.02em;
  }

  @media (max-width: 768px) {
    .story-finale { padding-block: var(--space-3xl); }
  }

  /* ══════════════════════════════════════════
     LQIP REVEAL
     ══════════════════════════════════════════ */
  .lqip-img.lqip-pending { opacity: 0; }
  .lqip-img:not(.lqip-pending) { transition: opacity 0.4s cubic-bezier(0.4, 0, 0.2, 1); }

  /* ══════════════════════════════════════════
     SCROLL REVEALS
     ══════════════════════════════════════════ */
  [data-reveal] {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }
  [data-reveal].is-visible {
    opacity: 1;
    transform: translateY(0);
  }
  [data-delay="1"] { transition-delay: 0.12s; }
  [data-delay="2"] { transition-delay: 0.24s; }
  [data-delay="3"] { transition-delay: 0.36s; }

  @media (prefers-reduced-motion: reduce) {
    [data-reveal] { opacity: 1; transform: none; transition: none; }
    .tl-card { opacity: 1 !important; transform: none !important; transition: none !important; }
    .tl-dot-core { background: var(--c-yellow) !important; border-color: var(--c-yellow) !important; }
    .tl-track-fill { transform: scaleY(1) !important; }
    .tl-dot-ring, .tl-cursor { display: none !important; }
    .tl-cap { opacity: 1 !important; transform: none !important; }
    .tl-cap-icon { animation: none !important; }
    .tl-connector { width: 30px !important; }
  }
</style>

<script>
  function initTimeline() {
    const timeline = document.getElementById('timeline');
    if (!timeline) return;

    const fill = timeline.querySelector<HTMLElement>('.tl-track-fill');
    const cursor = timeline.querySelector<HTMLElement>('.tl-cursor');
    const items = timeline.querySelectorAll<HTMLElement>('[data-tl-item]');
    const cap = timeline.querySelector<HTMLElement>('.tl-cap');
    if (!fill || !items.length) return;

    const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    if (prefersReduced) {
      items.forEach(el => el.classList.add('is-active'));
      cap?.classList.add('is-active');
      return; /* no .tl-ready — content stays visible as-is */
    }

    /* ── Progressive enhancement gate ──
     * Only hide elements AFTER confirming JS will reveal them.
     * If JS never reaches this point, content stays visible. */
    timeline.classList.add('tl-ready');

    /* ── Track fill + cursor ──
     * CSS animation-timeline handles this on the compositor thread
     * when supported. JS scroll listener is the fallback only. */
    const cssScrollDriven = CSS.supports('animation-timeline', 'view()');
    let onScroll: (() => void) | null = null;

    if (!cssScrollDriven) {
      function updateTrack() {
        const rect = timeline!.getBoundingClientRect();
        const viewH = window.innerHeight;
        const timelineTop = rect.top;
        const timelineH = rect.height;

        if (timelineTop > viewH) {
          fill!.style.transform = 'scaleY(0)';
          if (cursor) { cursor.classList.remove('is-visible'); }
          return;
        }

        const scrolled = viewH * 0.55 - timelineTop;
        const progress = Math.min(Math.max(scrolled / timelineH, 0), 1);
        fill!.style.transform = `scaleY(${progress})`;

        if (cursor) {
          if (progress > 0.01 && progress < 0.99) {
            cursor.classList.add('is-visible');
            cursor.style.transform = `translateY(${progress * 100}%)`;
          } else {
            cursor.classList.remove('is-visible');
          }
        }
      }

      let ticking = false;
      onScroll = () => {
        if (!ticking) {
          ticking = true;
          requestAnimationFrame(() => {
            updateTrack();
            ticking = false;
          });
        }
      };

      window.addEventListener('scroll', onScroll, { passive: true });
      updateTrack();
    }

    /* ── Intersection observer for items ── */
    const isMobile = window.innerWidth <= 640;
    let activatedCount = 0;
    const itemObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting && !entry.target.classList.contains('is-active')) {
          const el = entry.target as HTMLElement;
          const delay = Math.min(activatedCount * 100, 250);
          setTimeout(() => el.classList.add('is-active'), delay);
          activatedCount++;
          itemObserver.unobserve(el);
        }
      });
    }, {
      threshold: isMobile ? 0.08 : 0.15,
      rootMargin: isMobile ? '0px 0px -30px 0px' : '0px 0px -80px 0px',
    });

    items.forEach(el => itemObserver.observe(el));

    /* ── End cap observer ── */
    let capObserver: IntersectionObserver | null = null;
    if (cap) {
      capObserver = new IntersectionObserver((entries) => {
        if (entries[0].isIntersecting) {
          cap.classList.add('is-active');
          capObserver!.disconnect();
        }
      }, { threshold: 0.3 });
      capObserver.observe(cap);
    }

    /* ── Safety net — force-reveal if observer misses items ── */
    const safety = setTimeout(() => {
      items.forEach(el => {
        if (!el.classList.contains('is-active')) {
          el.classList.add('is-active');
          itemObserver.unobserve(el);
        }
      });
      if (cap && !cap.classList.contains('is-active')) cap.classList.add('is-active');
    }, 4000);

    /* ── Cleanup for Astro View Transitions ── */
    document.addEventListener('astro:before-swap', () => {
      if (onScroll) window.removeEventListener('scroll', onScroll);
      itemObserver.disconnect();
      capObserver?.disconnect();
      clearTimeout(safety);
    }, { once: true });
  }

  document.addEventListener('astro:page-load', initTimeline);
</script>


