---
/**
 * Leadership — Team & Board of Directors
 * Premium editorial layout with founders feature, scroll reveal,
 * B&W→color photo treatment, JSON-LD structured data, and breadcrumbs.
 */
import PageLayout from '../../layouts/PageLayout.astro';
import { getCollection } from 'astro:content';
import { cfUrl, cfSrcset, cfLqip } from '../../utils/cf-image';
import { getSiteStats } from '../../utils/stats';

const PLACEHOLDER = 'brand-logo-circle-display';

const allTeam = await getCollection('team');
const founders = allTeam
  .filter(t => t.data.group === 'team' && t.data.title?.toLowerCase().includes('co-founder'))
  .sort((a, b) => (a.data.order || 0) - (b.data.order || 0));
const staff = allTeam
  .filter(t => t.data.group === 'team' && !t.data.title?.toLowerCase().includes('co-founder'))
  .sort((a, b) => (a.data.order || 0) - (b.data.order || 0));
const boardMembers = allTeam
  .filter(t => t.data.group === 'board')
  .sort((a, b) => (a.data.order || 0) - (b.data.order || 0));

/* ── Stats ── */
const computed = await getSiteStats();

/* ── Breadcrumbs (BaseLayout emits WebPage + BreadcrumbList schema) ── */
const pageDesc = `Meet the team and board of directors behind Sunshine on a Ranney Day, the people who make dream rooms possible for ${computed.totalKids}+ children with special needs in the greater Atlanta area.`;
const breadcrumbs = [
  { name: 'Home', url: 'https://sunshineonaranneyday.com/' },
  { name: 'About', url: 'https://sunshineonaranneyday.com/about/' },
  { name: 'Leadership', url: 'https://sunshineonaranneyday.com/about/leadership/' },
];
---

<PageLayout
  title="Our Team & Board of Directors"
  description={pageDesc}
  breadcrumbs={breadcrumbs}
  heroLabel="About"
  heroTitle={'Our <em class="highlight">Leadership</em>'}
  heroSubtitle="The people behind the mission"
>

  <!-- ══════ IMPACT RIBBON ══════ -->
  <section class="impact-ribbon" aria-label="Impact numbers">
    <div class="ribbon-inner">
      <div class="ribbon-stat" data-reveal>
        <span class="ribbon-num">{computed.totalRooms}+</span>
        <span class="ribbon-label">Rooms Completed</span>
      </div>
      <div class="ribbon-divider" role="presentation"></div>
      <div class="ribbon-stat" data-reveal data-delay="1">
        <span class="ribbon-num">{computed.totalKids}+</span>
        <span class="ribbon-label">Children Served</span>
      </div>
      <div class="ribbon-divider" role="presentation"></div>
      <div class="ribbon-stat" data-reveal data-delay="2">
        <span class="ribbon-num">{computed.years}</span>
        <span class="ribbon-label">Years of Impact</span>
      </div>
    </div>
  </section>

  <!-- ══════ BOARD OF DIRECTORS ══════ -->
  <section class="section section--cream" id="board" aria-labelledby="board-heading">
    <div class="container">
      <div class="section-intro" data-reveal>
        <h2 class="section-label" id="board-heading">Board of Directors</h2>
        <p class="section-tagline">Community leaders guiding our <span class="highlight">mission and growth</span></p>
        <p class="section-desc">
          Industry experts who volunteer their time and expertise to ensure we serve more families, more effectively.
        </p>
      </div>

      <div class="board-grid">
        {boardMembers.map((member, i) => (
          <div class="board-card" data-reveal data-delay={String(Math.min(i, 4))}>
            <div class="board-photo" style={member.data.photo ? `background-image:url(${cfLqip(member.data.photo)});background-size:cover;background-position:center` : undefined}>
              {member.data.photo ? (
                <img
                  src={cfUrl(member.data.photo, { w: 200, h: 200, fit: 'cover', gravity: 'face' })}
                  srcset={cfSrcset(member.data.photo, [140, 200, 280, 400], { fit: 'cover', gravity: 'face', aspectRatio: 1 })}
                  sizes="(max-width: 600px) 140px, 200px"
                  alt={`${member.data.name}, ${member.data.title}`}
                  width="200" height="200"
                  loading="lazy"
                  decoding="async"
                />
              ) : (
                <div class="photo-placeholder">
                  <img src={cfUrl(PLACEHOLDER, { w: 256 })} alt="" class="placeholder-icon" width="256" height="256" loading="lazy" decoding="async" />
                </div>
              )}
            </div>
            <h3 class="board-name">{member.data.name}</h3>
            <p class="board-title">{member.data.title}</p>
            {member.data.organization && (
              <p class="board-org">{member.data.organization}</p>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ══════ MISSION QUOTE ══════ -->
  <section class="quote-break" aria-label="Mission quote">
    <div class="container">
      <blockquote class="mission-quote" data-reveal>
        <svg class="mission-quote-mark" aria-hidden="true" width="48" height="36" viewBox="0 0 32 24" fill="none">
          <path d="M0 24V14.4C0 10.4 0.8 7.2 2.4 4.8C4.1 2.4 6.9 0.5 10.8 0L12 3.6C9.7 4.3 8.1 5.4 7.1 6.9C6.1 8.4 5.6 10.1 5.6 12H12V24H0ZM20 24V14.4C20 10.4 20.8 7.2 22.4 4.8C24.1 2.4 26.9 0.5 30.8 0L32 3.6C29.7 4.3 28.1 5.4 27.1 6.9C26.1 8.4 25.6 10.1 25.6 12H32V24H20Z" fill="currentColor"/>
        </svg>
        <p>Every child deserves a space that feels like their own — a room that says <em>'you matter.'</em></p>
        <cite>Holly & Peter Ranney, Co-Founders</cite>
      </blockquote>
    </div>
  </section>

  <!-- ══════ OUR TEAM ══════ -->
  <section class="section section--white" id="our-team" aria-labelledby="team-heading">
    <div class="container">
      <div class="section-intro" data-reveal>
        <h2 class="section-label" id="team-heading">Our Team</h2>
        <p class="section-tagline">The dedicated crew that turns <span class="highlight">dreams into reality</span></p>
        <p class="section-desc">
          From design and construction to operations and community outreach.
        </p>
      </div>

      <!-- Founders row — wider 2-up layout -->
      <div class="founders-row">
        {founders.map((member, i) => (
          <div class="founder-card" data-reveal data-delay={String(i)}>
            <div class="founder-photo-wrap">
              <div class="founder-photo" style={member.data.photo ? `background-image:url(${cfLqip(member.data.photo)});background-size:cover;background-position:center` : undefined}>
                {member.data.photo ? (
                  <img
                    src={cfUrl(member.data.photo, { w: 220, h: 220, fit: 'cover', gravity: 'face' })}
                    srcset={cfSrcset(member.data.photo, [160, 220, 320, 440], { fit: 'cover', gravity: 'face', aspectRatio: 1 })}
                    sizes="(max-width: 600px) 160px, 220px"
                    alt={`${member.data.name}, ${member.data.title}`}
                    width="220" height="220"
                    loading="eager"
                    decoding="async"
                  />
                ) : (
                  <div class="photo-placeholder">
                    <img src={cfUrl(PLACEHOLDER, { w: 256 })} alt="" class="placeholder-icon" width="256" height="256" loading="lazy" decoding="async" />
                  </div>
                )}
              </div>
              <div class="founder-ring" aria-hidden="true"></div>
            </div>
            <span class="founder-badge">Co-Founder</span>
            <h3 class="founder-name">{member.data.name}</h3>
            <p class="founder-title">{member.data.title.replace(/Co-Founder\/?/i, '').trim()}</p>
          </div>
        ))}
      </div>

      <!-- Staff grid -->
      <div class="team-grid">
        {staff.map((member, i) => (
          <div class="member-card" data-reveal data-delay={String(Math.min(i, 3))}>
            <div class="member-photo" style={member.data.photo ? `background-image:url(${cfLqip(member.data.photo)});background-size:cover;background-position:center` : undefined}>
              {member.data.photo ? (
                <img
                  src={cfUrl(member.data.photo, { w: 220, h: 220, fit: 'cover', gravity: 'face' })}
                  srcset={cfSrcset(member.data.photo, [160, 220, 320, 440], { fit: 'cover', gravity: 'face', aspectRatio: 1 })}
                  sizes="(max-width: 600px) 160px, 220px"
                  alt={`${member.data.name}, ${member.data.title}`}
                  width="220" height="220"
                  loading="lazy"
                  decoding="async"
                />
              ) : (
                <div class="photo-placeholder">
                  <img src={cfUrl(PLACEHOLDER, { w: 256 })} alt="" class="placeholder-icon" width="256" height="256" loading="lazy" decoding="async" />
                </div>
              )}
            </div>
            <h3 class="member-name">{member.data.name}</h3>
            <p class="member-title">{member.data.title}</p>
            {member.data.organization && (
              <p class="member-org">{member.data.organization}</p>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ══════ CTA ══════ -->
  <section class="section section--dark" slot="cta">
    <div class="container" style="text-align: center;">
      <div data-reveal>
        <h2 class="cta-heading">
          Want to <span class="highlight">join the mission</span>?
        </h2>
        <p class="cta-text">
          We're always looking for people who want to volunteer,
          partner, or support our work in other ways.
        </p>
        <div class="cta-buttons">
          <a href="/donate/" class="btn btn-primary btn-lg">Donate</a>
          <a href="/partners/" class="btn btn-ghost-light btn-lg">Become a Partner</a>
        </div>
      </div>
    </div>
  </section>
</PageLayout>

<style>
  /* ═══════════════════════════════════════════
     SECTION INTRO
     ═══════════════════════════════════════════ */
  .section-intro {
    text-align: center;
    max-width: 640px;
    margin-inline: auto;
    margin-bottom: var(--space-3xl);
  }

  .section-tagline {
    font-family: var(--font-display);
    font-size: clamp(1.35rem, 3vw, 1.75rem);
    font-weight: 700;
    color: var(--c-text);
    line-height: 1.3;
    margin-bottom: var(--space-sm);
  }

  .section-desc {
    font-size: 1.05rem;
    color: var(--c-text-muted);
    line-height: 1.7;
  }

  /* ═══════════════════════════════════════════
     IMPACT RIBBON
     ═══════════════════════════════════════════ */
  .impact-ribbon {
    background: var(--c-dark);
    padding-block: var(--space-2xl);
  }

  .ribbon-inner {
    max-width: var(--max-width);
    margin-inline: auto;
    padding-inline: var(--page-padding);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: clamp(var(--space-xl), 5vw, var(--space-4xl));
  }

  .ribbon-stat { text-align: center; }

  .ribbon-num {
    display: block;
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 700;
    color: var(--c-yellow);
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .ribbon-label {
    font-size: 0.78rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: rgba(255, 255, 255, 0.7);
    margin-top: 0.4rem;
    display: block;
  }

  .ribbon-divider {
    width: 1px;
    height: 48px;
    background: rgba(255, 255, 255, 0.1);
    flex-shrink: 0;
  }

  /* ═══════════════════════════════════════════
     BOARD OF DIRECTORS — Centered portrait cards
     ═══════════════════════════════════════════ */
  .board-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-2xl) var(--space-xl);
    max-width: 1000px;
    margin-inline: auto;
  }

  .board-card {
    text-align: center;
  }

  .board-photo {
    width: 180px;
    max-width: 100%;
    aspect-ratio: 1;
    height: auto;
    border-radius: 50%;
    overflow: hidden;
    margin: 0 auto var(--space-md);
    background: var(--c-warm-gray);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
    transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1),
                box-shadow 0.5s ease;
  }

  @media (hover: hover) {
    .board-card:hover .board-photo {
      transform: scale(1.04) translateY(-4px);
      box-shadow: 0 16px 48px rgba(0, 0, 0, 0.1), 0 0 0 3px rgba(255, 218, 36, 0.25);
    }
  }

  .board-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: filter 0.6s ease;
  }

  @media (hover: hover) {
    .board-photo img {
      filter: grayscale(100%) contrast(1.05);
    }
    .board-card:hover .board-photo img {
      filter: grayscale(0%) contrast(1);
    }
  }

  .board-name {
    font-family: var(--font-display);
    font-size: 1.05rem;
    font-weight: 700;
    margin-bottom: 0.15rem;
    color: var(--c-text);
  }

  .board-title {
    font-size: 0.85rem;
    color: var(--c-text-muted);
    font-weight: 500;
    line-height: 1.4;
  }

  .board-org {
    font-size: 0.78rem;
    color: var(--c-text-light);
    margin-top: 0.2rem;
    line-height: 1.4;
  }

  /* ═══════════════════════════════════════════
     MISSION QUOTE BREAK
     ═══════════════════════════════════════════ */
  .quote-break {
    background: var(--c-dark);
    padding-block: var(--space-4xl);
  }

  .mission-quote {
    max-width: 680px;
    margin-inline: auto;
    text-align: center;
  }

  .mission-quote-mark {
    color: var(--c-yellow);
    opacity: 0.3;
    margin-bottom: var(--space-lg);
  }

  .mission-quote p {
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 2.8vw, 1.65rem);
    font-style: italic;
    font-weight: 500;
    color: var(--c-white);
    line-height: 1.6;
    margin-bottom: var(--space-lg);
  }

  .mission-quote em {
    color: var(--c-yellow);
    font-style: italic;
  }

  .mission-quote cite {
    font-style: normal;
    font-size: 0.82rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.72);
  }

  /* ═══════════════════════════════════════════
     FOUNDERS ROW — 2-up centered above grid
     ═══════════════════════════════════════════ */
  .founders-row {
    display: flex;
    justify-content: center;
    gap: clamp(var(--space-3xl), 8vw, 8rem);
    margin-bottom: var(--space-2xl);
  }

  .founder-card {
    text-align: center;
    max-width: 320px;
  }

  .founder-photo-wrap {
    position: relative;
    width: fit-content;
    margin: 0 auto var(--space-lg);
  }

  .founder-photo {
    width: 220px;
    max-width: 100%;
    aspect-ratio: 1;
    height: auto;
    border-radius: 50%;
    overflow: hidden;
    background: var(--c-warm-gray);
    box-shadow:
      0 20px 40px -8px rgba(0, 0, 0, 0.12),
      0 0 0 1px rgba(0, 0, 0, 0.03);
    transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1),
                box-shadow 0.5s ease;
  }

  @media (hover: hover) {
    .founder-card:hover .founder-photo {
      transform: scale(1.04) translateY(-4px);
      box-shadow: 0 24px 56px rgba(0, 0, 0, 0.14), 0 0 0 3px rgba(255, 218, 36, 0.3);
    }
  }

  .founder-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: filter 0.6s ease;
  }

  @media (hover: hover) {
    .founder-photo img {
      filter: grayscale(100%) contrast(1.05);
    }
    .founder-card:hover .founder-photo img {
      filter: grayscale(0%) contrast(1);
    }
  }

  .founder-ring {
    position: absolute;
    inset: -8px;
    border-radius: 50%;
    border: 2px solid var(--c-yellow);
    opacity: 0;
    transform: scale(0.97);
    transition: opacity 0.6s ease, transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    pointer-events: none;
  }

  @media (hover: hover) {
    .founder-card:hover .founder-ring {
      opacity: 0.5;
      transform: scale(1);
    }
  }

  .founder-badge {
    display: inline-block;
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--c-white);
    background: var(--c-dark);
    padding: 0.35em 1.1em;
    border-radius: 100px;
    margin-bottom: var(--space-sm);
  }

  .founder-name {
    font-family: var(--font-display);
    font-size: 1.15rem;
    font-weight: 700;
    margin-bottom: 0.2rem;
    color: var(--c-text);
    line-height: 1.2;
  }

  .founder-title {
    font-size: 0.88rem;
    color: var(--c-text-muted);
    font-weight: 500;
    line-height: 1.4;
    margin-bottom: var(--space-xs);
  }


/* ═══════════════════════════════════════════
     TEAM GRID — B&W→Color on hover
     ═══════════════════════════════════════════ */
  .team-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-3xl) var(--space-2xl);
    max-width: 940px;
    margin-inline: auto;
  }

  .member-card {
    text-align: center;
  }

  .member-photo {
    width: 220px;
    max-width: 100%;
    aspect-ratio: 1;
    height: auto;
    margin: 0 auto var(--space-lg);
    border-radius: 50%;
    overflow: hidden;
    background: var(--c-warm-gray);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
    transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1),
                box-shadow 0.5s ease;
  }

  @media (hover: hover) {
    .member-card:hover .member-photo {
      transform: scale(1.04) translateY(-4px);
      box-shadow: 0 16px 48px rgba(0, 0, 0, 0.1), 0 0 0 3px rgba(255, 218, 36, 0.25);
    }
  }

  .member-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: filter 0.6s ease;
  }

  @media (hover: hover) {
    .member-photo img {
      filter: grayscale(100%) contrast(1.05);
    }
    .member-card:hover .member-photo img {
      filter: grayscale(0%) contrast(1);
    }
  }

  .member-name {
    font-family: var(--font-display);
    font-size: 1.15rem;
    font-weight: 700;
    margin-bottom: 0.2rem;
    color: var(--c-text);
  }

  .member-title {
    font-size: 0.9rem;
    color: var(--c-text-muted);
    font-weight: 500;
    line-height: 1.4;
  }

  .member-org {
    font-size: 0.82rem;
    color: var(--c-text-light);
    margin-top: 0.2rem;
  }

  /* ═══════════════════════════════════════════
     PLACEHOLDERS (shared)
     ═══════════════════════════════════════════ */
  .photo-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--c-warm-gray);
  }

  .photo-placeholder .placeholder-icon {
    width: 60%;
    height: 60%;
    object-fit: contain;
    opacity: 0.5;
  }


  /* ═══════════════════════════════════════════
     CTA
     ═══════════════════════════════════════════ */
  .cta-heading {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 4vw, 2.25rem);
    color: var(--c-white);
    margin-bottom: var(--space-md);
  }

  .cta-text {
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.7);
    max-width: 560px;
    margin-inline: auto;
    margin-bottom: var(--space-xl);
    line-height: 1.7;
  }

  .cta-buttons {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    flex-wrap: wrap;
  }

  /* ═══════════════════════════════════════════
     SCROLL REVEAL
     ═══════════════════════════════════════════ */
  :global(.js-reveal) [data-reveal] {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1),
                transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
  }

  :global(.js-reveal) [data-reveal].is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  :global(.js-reveal) [data-reveal][data-delay="1"] { transition-delay: 0.12s; }
  :global(.js-reveal) [data-reveal][data-delay="2"] { transition-delay: 0.24s; }
  :global(.js-reveal) [data-reveal][data-delay="3"] { transition-delay: 0.3s; }
  :global(.js-reveal) [data-reveal][data-delay="4"] { transition-delay: 0.36s; }

  @media (prefers-reduced-motion: reduce) {
    :global(.js-reveal) [data-reveal] {
      opacity: 1;
      transform: none;
      transition: none;
    }

    .founder-photo,
    .member-photo,
    .board-photo {
      transition: none;
    }

    .founder-photo img,
    .member-photo img,
    .board-photo img {
      filter: none !important;
      transition: none;
    }
  }

  /* ═══════════════════════════════════════════
     RESPONSIVE
     ═══════════════════════════════════════════ */
  @media (max-width: 768px) {
    .founders-row {
      gap: var(--space-2xl);
    }

    .founder-photo { width: 160px; }

    .team-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: var(--space-xl);
    }

    .member-photo { width: 160px; }

    .board-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: var(--space-xl);
    }

    .board-photo { width: 150px; }
  }

  @media (max-width: 600px) {
    .founders-row {
      gap: var(--space-lg);
    }

    .founder-photo { width: 140px; }
    .founder-name { font-size: 1.05rem; }
    .founder-badge { font-size: 0.6rem; }

    .team-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-xl) var(--space-lg);
    }

    .member-photo { width: 140px; }
    .member-name { font-size: 1.05rem; }

    .board-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-xl) var(--space-lg);
    }

    .board-photo { width: 130px; }

    .quote-break {
      padding-block: var(--space-3xl);
    }

    .ribbon-inner {
      flex-wrap: wrap;
      gap: var(--space-lg) var(--space-xl);
    }

    .ribbon-divider { display: none; }

    .ribbon-stat {
      flex: 0 0 calc(33% - var(--space-lg));
    }

    .section-intro {
      margin-bottom: var(--space-2xl);
    }
  }

  @media (max-width: 380px) {
    .founder-photo { width: 110px; }

    .team-grid {
      gap: var(--space-lg) var(--space-md);
    }

    .member-photo { width: 110px; }

    .board-grid {
      gap: var(--space-lg) var(--space-md);
    }

    .board-photo { width: 110px; }

    .board-name { font-size: 0.95rem; }
    .board-title { font-size: 0.78rem; }
  }
</style>

<script>
  /* ── Scroll Reveal — JS-dependent progressive enhancement ── */
  const reveals = document.querySelectorAll('[data-reveal]');
  if (reveals.length && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    /* Enable reveal styles only when JS is available */
    document.documentElement.classList.add('js-reveal');

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.15, rootMargin: '0px 0px -60px 0px' });

    reveals.forEach(el => observer.observe(el));
  }
  /* No else needed — without .js-reveal, content is visible by default */
</script>
