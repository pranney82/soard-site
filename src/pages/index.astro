---
/**
 * Home Page — Sunshine on a Ranney Day
 * Cinematic hero, editorial layout, scroll reveals
 * Performance: real CF Images, optimized animations, contain
 * SEO: JSON-LD NonprofitOrganization + WebSite, semantic HTML5
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import MediaLogos from '../components/ui/MediaLogos.astro';
import PartnerShowcase from '../components/ui/PartnerShowcase.astro';
import { getCollection } from 'astro:content';
import { getSiteStats } from '../utils/stats';
import { getPublishedKids } from '../utils/collections';

import { CF_BASE as CF, cfId as extractCfId, cfRaw, cfSrcset, cfLqip } from '../utils/cf-image';

/* ── Site settings (all editable from admin) ── */
import siteSettings from '../content/site/settings.json';
import financialSnapshot from '../content/site/financial-snapshot.json';

/* ── Auto-computed stats from content collections ── */
const computed = await getSiteStats();
const stats = {
  totalKids: computed.totalKids,
  totalRooms: computed.totalRooms,
  years: computed.years,
};

/* ── Hero (from site settings) ── */
const heroEyebrow = siteSettings.homeHero?.eyebrow || '501(c)(3) Nonprofit · Atlanta, GA · Since 2012';
const heroLine1 = siteSettings.homeHero?.headlineLine1 || 'Building <em>dreams</em>,';
const heroLine2 = siteSettings.homeHero?.headlineLine2 || 'one room at a time';
const heroHeadline = `${heroLine1}<br>${heroLine2}`;
const heroSub = siteSettings.homeHero?.lead || 'Dream bedrooms, accessible bathrooms, and therapy rooms for children with special needs, at no cost to families.';
const heroCta1Label = siteSettings.homeHero?.cta1Label || 'Give Today';
const heroCta1Link = siteSettings.homeHero?.cta1Link || '/donate';
const heroCta2Label = siteSettings.homeHero?.cta2Label || 'See Our Work';
const heroCta2Link = siteSettings.homeHero?.cta2Link || '#stories';

/* ── Hero Kid + Featured Story Kid (independent) ── */
const allKids = await getPublishedKids();

const heroSlug = siteSettings.heroKidSlug || siteSettings.featuredKidSlug || 'zyah';
const heroKid = allKids.find(k => k.id === heroSlug);
const hd = heroKid?.data;

const featuredSlug = siteSettings.featuredKidSlug || 'zyah';
const featuredKid = allKids.find(k => k.id === featuredSlug);
const fd = featuredKid?.data;

/* ── Kids Strip (from settings) ── */
const kidShowcaseData = siteSettings.kidShowcase || [
  { slug: 'liam', cfId: 'kids/liam/hero' },
  { slug: 'sophia', cfId: 'kids/sophia/hero' },
  { slug: 'jackson', cfId: 'kids/jackson/hero' },
  { slug: 'griffin', cfId: 'kids/griffin/hero' },
  { slug: 'madison', cfId: 'kids/madison/hero' },
  { slug: 'harper', cfId: 'kids/harper/hero' },
  { slug: 'noah', cfId: 'kids/noah/hero' },
  { slug: 'summer', cfId: 'kids/summer/hero' },
];
const kidShowcase = kidShowcaseData.map(item => {
  const kid = allKids.find(k => k.id === item.slug);
  const heroId = kid?.data?.heroImage ? extractCfId(kid.data.heroImage) : null;
  return {
    ...item,
    cfId: heroId || item.cfId || `kids/${item.slug}/hero`,
    name: kid?.data?.name || item.slug,
    age: kid?.data?.age,
    roomLabel: kid?.data?.roomTypes?.[0] || 'Dream Bedroom',
  };
});

/* ── Section photos (from settings) ── */
const sp = siteSettings.sectionPhotos || {};
const missionMainId = sp.missionMain || 'kids/eli/photo-01';
const missionAccentId = sp.missionAccent || 'kids/georgia/photo-01';
const heroPhotoId = siteSettings.heroKidPhotoId || `kids/${heroSlug}/hero`;
const storyPhotoId = siteSettings.featuredStoryImage || (fd?.heroImage ? extractCfId(fd.heroImage) : `kids/${featuredSlug}/hero`);
const processPhotoId = sp.process || `kids/${featuredSlug}/hero`;
const roomPhotos = {
  bedroom: sp.roomBedroom || 'kids/griffin/hero',
  bath: sp.roomBath || 'kids/zyah/hero',
  therapy: sp.roomTherapy || 'kids/jackson/hero',
};
/* ── Partners (dynamic count from collection + featured logos from settings) ── */
const partnerCollection = await getCollection('partners');
const totalPartners = partnerCollection.length;

const signaturePartners = partnerCollection
  .filter((p) => p.data.level === 'signature' && p.data.logo)
  .map((p) => ({ id: extractCfId(p.data.logo), name: p.data.name }));

const partnerLogos = signaturePartners;

/* ── Hero image URLs (DRY — preload + img must be byte-identical) ── */
/* Hero srcset: 4:5 aspect, gravity=face for kid photos, q=70 balances sharpness vs weight.
   CF Images auto-negotiates AVIF/WebP via Accept header — no format= needed.
   Preload <link> + <img> must use byte-identical srcset/sizes strings. */
const heroTransforms = 'fit=cover,gravity=face,q=70';
const heroImg320  = `${CF}/${heroPhotoId}/w=320,h=400,${heroTransforms}`;
const heroImg480  = `${CF}/${heroPhotoId}/w=480,h=600,${heroTransforms}`;
const heroImg640  = `${CF}/${heroPhotoId}/w=640,h=800,${heroTransforms}`;
const heroImg960  = `${CF}/${heroPhotoId}/w=960,h=1200,${heroTransforms}`;
const heroImg1280 = `${CF}/${heroPhotoId}/w=1280,h=1600,${heroTransforms}`;
const heroSrcset = `${heroImg320} 320w, ${heroImg480} 480w, ${heroImg640} 640w, ${heroImg960} 960w, ${heroImg1280} 1280w`;
/* Photo column: 0.85fr of 1400px grid ≈ 530px. 2× retina → 1060px. 1280w covers 2.4× comfortably. */
const heroSizes = '(max-width:480px) 80vw, (max-width:768px) 60vw, 530px';

import { getWebSiteSchema, getSiteNavigationSchema } from '../utils/schema';
const jsonLd = [getWebSiteSchema(), ...getSiteNavigationSchema()];
---

<BaseLayout
  title="Home"
  description="We build free dream bedrooms, accessible bathrooms, and therapy rooms for children with special needs in Atlanta. 501(c)(3) nonprofit. Donate or apply today."
  ogImage="/og/home.png"
  jsonLd={jsonLd}
>
  <!-- Preload hero LCP image — imagesrcset/imagesizes must match the <img> -->
  <link slot="head" rel="preload" as="image" imagesrcset={heroSrcset} imagesizes={heroSizes} fetchpriority="high" />

  <!-- ══════ HERO — Editorial ══════ -->
  <section class="hero" aria-label="Introduction">
    <div class="hero-inner">
      <div class="hero-text">
        <p class="hero-eyebrow">{heroEyebrow}</p>
        <h1 class="hero-headline" set:html={heroHeadline} />
        <p class="hero-sub" set:html={heroSub} />
        <div class="hero-ctas">
          <a href={heroCta1Link} class="btn btn-primary btn-hero-glow">{heroCta1Label}</a>
          <a href={heroCta2Link} class="btn btn-dark">{heroCta2Label} <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true" style="margin-left:2px"><path d="M8 3v10M3 8l5 5 5-5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg></a>
        </div>
      </div>
      <div class="hero-photo-wrap">
        <a href={`/kids/${heroSlug}/`} class="hero-photo">
          <img
            src={heroImg960}
            srcset={heroSrcset}
            sizes={heroSizes}
            alt={`${hd?.name || 'Zyah'}'s room reveal — a life-changing moment`}
            width="960" height="1200"
            loading="eager" fetchpriority="high" decoding="auto"
          />
          <span class="hero-photo-name">{hd?.name || 'Zyah'}, {Array.isArray(hd?.age) ? `ages ${hd.age.join(', ')}` : `age ${hd?.age || 7}`}</span>
        </a>
        <div class="hero-photo-accent" aria-hidden="true"></div>
      </div>
    </div>

  </section>

  <!-- ══════ IMPACT RIBBON ══════ -->
  <section class="ribbon" aria-label="Impact numbers">
    <div class="ribbon-inner">
      <div class="ribbon-stat" data-reveal>
        <span class="ribbon-num">{stats.totalRooms}+</span>
        <span class="ribbon-label">Room Makeovers</span>
      </div>
      <div class="ribbon-divider" role="presentation"></div>
      <div class="ribbon-stat" data-reveal data-delay="1">
        <span class="ribbon-num">{stats.totalKids}+</span>
        <span class="ribbon-label">Children Served</span>
      </div>
      <div class="ribbon-divider" role="presentation"></div>
      <div class="ribbon-stat" data-reveal data-delay="2">
        <span class="ribbon-num">{stats.years}</span>
        <span class="ribbon-label">Years of Impact</span>
      </div>
    </div>
  </section>

  <MediaLogos theme="dark" linked={true} />

  <!-- ══════ MISSION ══════ -->
  <section class="mission" aria-labelledby="mission-heading">
    <div class="mission-inner">
      <div class="mission-text" data-reveal>
        <div class="section-label">Our Mission</div>
        <h2 id="mission-heading">Every child deserves a space that makes them feel <em class="highlight">safe</em>, <em class="highlight">seen</em>, and <em class="highlight">loved</em>.</h2>
        <p class="mission-body">A bedroom that feels like theirs. A bathroom that works for them. A therapy room built around who they are. For children with special needs, the right space changes everything. It brings independence, comfort, and confidence to daily life for the entire family.</p>
        <p class="mission-body">Since 2012, we've partnered with designers, contractors, and families across Greater Atlanta to create over {stats.totalRooms} life-changing spaces. Every project is unique because every child is unique.</p>
      </div>
      <div class="mission-visual" role="presentation">
        <div class="mission-photo mission-photo--main" data-reveal style={`background-image:url('${cfLqip(missionMainId)}')`}>
          <img src={`${CF}/${missionMainId}/w=600,q=65`} srcset={`${CF}/${missionMainId}/w=360,q=65 360w, ${CF}/${missionMainId}/w=480,q=65 480w, ${CF}/${missionMainId}/w=600,q=65 600w`} sizes="(max-width:768px) 65vw, 30vw" alt={`A completed SOARD room makeover — one of ${stats.totalRooms}+ spaces built for children with special needs`} width="800" height="600" loading="lazy" decoding="async" class="lqip-img" />
        </div>
        <div class="mission-photo mission-photo--accent" data-reveal data-delay="1" style={`background-image:url('${cfLqip(missionAccentId)}')`}>
          <img src={`${CF}/${missionAccentId}/w=600,q=65`} srcset={`${CF}/${missionAccentId}/w=280,q=65 280w, ${CF}/${missionAccentId}/w=400,q=65 400w, ${CF}/${missionAccentId}/w=600,q=65 600w, ${CF}/${missionAccentId}/w=800,q=65 800w`} sizes="(max-width:768px) 52vw, 24vw" alt="A child smiling in their newly designed dream bedroom by Sunshine on a Ranney Day" width="600" height="450" loading="lazy" decoding="async" class="lqip-img" />
        </div>
      </div>
    </div>
  </section>

  <!-- ══════ ROOM TYPES — Editorial magazine ══════ -->
  <section class="rooms" aria-labelledby="rooms-heading">
    <div class="rooms-inner">
      <div class="rooms-header" data-reveal>
        <div class="section-label">What We Build</div>
        <h2 id="rooms-heading">Three types of spaces,<br/>one purpose: <em class="highlight">independence</em></h2>
      </div>

      <!-- Three room-type cards -->
      <div class="rm-trio">
        <article class="rm-card" data-reveal>
          <a href="/kids/?type=type-accessible-bathroom" class="rm-card-link" aria-label="Accessible bathrooms — see our work">
            <div class="rm-card-img rm-parallax" style={`background-image:url('${cfLqip(roomPhotos.bath)}')`}>
              <img
                src={`${CF}/${roomPhotos.bath}/w=700,h=875,fit=cover,q=80`}
                srcset={cfSrcset(roomPhotos.bath, [360, 500, 700, 1000, 1400], { fit: 'cover', aspectRatio: 5/4, quality: 80 })}
                sizes="(max-width:600px) 82vw, (max-width:900px) 46vw, 30vw"
                alt="An accessible bathroom built by SOARD with roll-in shower, grab bars, and lowered vanity" width="700" height="875"
                loading="lazy" decoding="async" class="lqip-img"
              />
            </div>
            <div class="rm-card-text">
              <span class="rm-num" aria-hidden="true">01</span>
              <h3>Accessible Bathrooms</h3>
              <div class="rm-rule"></div>
              <p>Roll-in showers, grab bars, and lowered vanities that give children independence and give families relief from daily struggles.</p>
              <span class="rm-cta">See our work <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true"><path d="M4 10h12M11 5l5 5-5 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg></span>
            </div>
          </a>
        </article>
        <article class="rm-card" data-reveal data-delay="1">
          <a href="/kids/?type=type-dream-bedroom" class="rm-card-link" aria-label="Dream bedrooms — see our work">
            <div class="rm-card-img rm-parallax" style={`background-image:url('${cfLqip(roomPhotos.bedroom)}')`}>
              <img
                src={`${CF}/${roomPhotos.bedroom}/w=700,h=875,fit=cover,q=80`}
                srcset={cfSrcset(roomPhotos.bedroom, [360, 500, 700, 1000, 1400], { fit: 'cover', aspectRatio: 5/4, quality: 80 })}
                sizes="(max-width:600px) 82vw, (max-width:900px) 46vw, 30vw"
                alt="A personalized dream bedroom designed around a child's interests and needs" width="700" height="875"
                loading="lazy" decoding="async" class="lqip-img"
              />
            </div>
            <div class="rm-card-text">
              <span class="rm-num" aria-hidden="true">02</span>
              <h3>Dream Bedrooms</h3>
              <div class="rm-rule"></div>
              <p>Personalized sanctuaries where children can escape, imagine, and simply be kids, designed around their unique interests and needs.</p>
              <span class="rm-cta">See our work <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true"><path d="M4 10h12M11 5l5 5-5 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg></span>
            </div>
          </a>
        </article>
        <article class="rm-card" data-reveal data-delay="2">
          <a href="/kids/?type=type-therapy-room" class="rm-card-link" aria-label="Therapy rooms — see our work">
            <div class="rm-card-img rm-parallax" style={`background-image:url('${cfLqip(roomPhotos.therapy)}')`}>
              <img
                src={`${CF}/${roomPhotos.therapy}/w=700,h=875,fit=cover,q=80`}
                srcset={cfSrcset(roomPhotos.therapy, [360, 500, 700, 1000, 1400], { fit: 'cover', aspectRatio: 5/4, quality: 80 })}
                sizes="(max-width:600px) 82vw, (max-width:900px) 46vw, 30vw"
                alt="A calming sensory therapy room with dimmable lights, swing, and textured walls" width="700" height="875"
                loading="lazy" decoding="async" class="lqip-img"
              />
            </div>
            <div class="rm-card-text">
              <span class="rm-num" aria-hidden="true">03</span>
              <h3>Therapy Rooms</h3>
              <div class="rm-rule"></div>
              <p>Calming retreats with dimmable lights, swings, crash pads, and textured walls, where children can decompress and develop.</p>
              <span class="rm-cta">See our work <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true"><path d="M4 10h12M11 5l5 5-5 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg></span>
            </div>
          </a>
        </article>
      </div>
    </div>
  </section>

  <!-- ══════ FEATURED STORY ══════ -->
  <section class="story" id="stories" aria-labelledby="story-heading">
    <div class="story-inner">
      <div class="story-split">
        <figure class="story-photo" style={`background-image:url('${cfLqip(storyPhotoId)}')`}>
          <img src={`${CF}/${storyPhotoId}/w=700,q=65`} srcset={`${CF}/${storyPhotoId}/w=400,q=65 400w, ${CF}/${storyPhotoId}/w=600,q=65 600w, ${CF}/${storyPhotoId}/w=700,q=65 700w`} sizes="(max-width:768px) 100vw, 40vw" alt={`${fd?.name || 'Zyah'}'s room reveal`} width="900" height="600" loading="lazy" decoding="async" class="story-photo-img lqip-img" />
          <div class="story-photo-tag">Featured</div>
        </figure>
        <div class="story-content" data-reveal>
          <div class="section-label">Featured Story</div>
          <h2 id="story-heading">{siteSettings.featuredStoryHeadline || `${fd?.name || 'Zyah'}'s new space gave them independence, and their family peace of mind.`}</h2>
          <p>{siteSettings.featuredStoryDescription || `${fd?.name || 'Zyah'}, ${Array.isArray(fd?.age) ? `ages ${fd.age.join(', ')}` : `age ${fd?.age || 7}`}, lives with ${fd?.diagnosis || 'Spinal Muscular Atrophy'}. Our team transformed their space into an accessible, joyful haven with custom features designed around their unique needs and personality.`}</p>
          <a href={`/kids/${featuredSlug}/`} class="btn btn-dark btn-lg story-cta">Read {fd?.name || 'Zyah'}'s Full Story →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════ PROCESS ══════ -->
  <section class="process" aria-labelledby="process-heading">
    <div class="process-inner">
      <div class="process-header" data-reveal>
        <div class="section-label">The Process</div>
        <h2 id="process-heading">From first call to <em class="highlight">big reveal</em></h2>
      </div>

      <div class="process-split">
        <div class="process-photo" data-reveal style={`background-image:url('${cfLqip(processPhotoId)}')`}>
          <img src={`${CF}/${processPhotoId}/w=960,fit=scale-down,gravity=face,q=70`} alt={`${fd?.name || 'A child'} sees their new room for the first time`} width="960" height="640" loading="lazy" decoding="async" class="lqip-img" />
        </div>

        <div class="process-content">
          <ol class="process-timeline" role="list">
            <li class="process-step" data-reveal data-delay="1">
              <span class="process-num" aria-hidden="true">01</span>
              <h3>Family Applies</h3>
              <p>Families tell us about their child, their needs, and their dreams for a new space.</p>
            </li>
            <li class="process-step" data-reveal data-delay="2">
              <span class="process-num" aria-hidden="true">02</span>
              <h3>Design & Plan</h3>
              <p>Our designer partners create a one-of-a-kind room concept tailored to the child's personality and needs.</p>
            </li>
            <li class="process-step" data-reveal data-delay="3">
              <span class="process-num" aria-hidden="true">03</span>
              <h3>Build & Transform</h3>
              <p>Construction partners bring the design to life.</p>
            </li>
            <li class="process-step process-step--highlight" data-reveal data-delay="4">
              <span class="process-num" aria-hidden="true">04</span>
              <span class="process-badge">The best part</span>
              <h3>The Big Reveal</h3>
              <p>The child walks in and sees a room designed just for them. Everything changes in that moment.</p>
            </li>
          </ol>
          <div class="process-cta" data-reveal>
            <a href="/partners/" class="btn btn-primary">Become a Partner →</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════ MORE KIDS ══════ -->
  <section class="kids-strip" aria-labelledby="kids-heading">
    <div class="kids-strip-header" data-reveal>
      <div>
        <div class="section-label">More Stories</div>
        <h2 id="kids-heading">Every room changes a <em class="highlight">life</em></h2>
      </div>
      <a href="/kids/" class="kids-strip-all">See all {stats.totalKids}+ kids →</a>
    </div>
    <div class="kids-scroll-wrap">
      <ul class="kids-scroll">
        {kidShowcase.map(kid => (
          <li class="kid-thumb-item">
            <a href={`/kids/${kid.slug}/`} class="kid-thumb">
              <div class="kid-thumb-photo" style={`background-image:url('${cfLqip(kid.cfId)}')`}>
                <img src={`${CF}/${kid.cfId}/w=440,h=440,fit=cover,q=65`} alt={`${kid.name}'s room reveal`} width="220" height="220" loading="lazy" decoding="async" class="lqip-img" />
              </div>
              <h3 class="kid-thumb-name">{kid.name}</h3>
              <p>{kid.roomLabel}{kid.age ? ` · ${Array.isArray(kid.age) && kid.age.length > 1 ? `Ages ${kid.age.join(', ')}` : `Age ${Array.isArray(kid.age) ? kid.age[0] : kid.age}`}` : ''}</p>
            </a>
          </li>
        ))}
      </ul>
      <button class="kids-scroll-btn kids-scroll-btn--prev" aria-label="Scroll left" type="button">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m15 18-6-6 6-6"/></svg>
      </button>
      <button class="kids-scroll-btn kids-scroll-btn--next" aria-label="Scroll right" type="button">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m9 18 6-6-6-6"/></svg>
      </button>
    </div>
  </section>

  <!-- ══════ TRUST ══════ -->
  <section class="trust" aria-labelledby="trust-heading">
    <div class="trust-inner">
      <div class="trust-header" data-reveal>
        <div class="section-label" style="color:var(--c-yellow)">Trust & Transparency</div>
        <h2 id="trust-heading">We're an open book. <em>Always.</em></h2>
        <p class="trust-subtitle">We believe you should know exactly where every dollar goes. Our financials are public, our mission is clear, and our track record speaks for itself.</p>
      </div>
      <div class="trust-grid">
        <div class="trust-card" data-reveal>
          <div class="trust-card-top"><span class="trust-big">{financialSnapshot.programPct}%</span><span class="trust-card-label">Program Services</span></div>
          <p>The vast majority of every dollar funds room materials, labor, and design for children's spaces.</p>
        </div>
        <div class="trust-card" data-reveal data-delay="1">
          <div class="trust-card-top"><span class="trust-big trust-big--icon"><svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 22a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h8a2.4 2.4 0 0 1 1.704.706l3.588 3.588A2.4 2.4 0 0 1 20 8v12a2 2 0 0 1-2 2z"/><path d="M14 2v5a1 1 0 0 0 1 1h5"/><path d="M10 9H8"/><path d="M16 13H8"/><path d="M16 17H8"/></svg></span><span class="trust-card-label">990 Public</span></div>
          <p>Our IRS Form 990 tax returns are available for anyone to review, anytime.</p>
        </div>
        <div class="trust-card" data-reveal data-delay="2">
          <div class="trust-card-top"><span class="trust-big trust-big--icon"><svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z"/><path d="m9 12 2 2 4-4"/></svg></span><span class="trust-card-label">501(c)(3)</span></div>
          <p>Registered nonprofit. EIN 45-4773997. All donations are fully tax-deductible.</p>
        </div>
      </div>
      <div class="trust-cta" data-reveal><a href="/about/financials/" class="btn btn-ghost-light">View Our Financials →</a></div>
    </div>
  </section>

  <!-- ══════ PARTNERS ══════ -->
  <PartnerShowcase logos={partnerLogos} totalPartners={totalPartners} />

  <!-- ══════ FINAL CTA ══════ -->
  <section class="finale" aria-labelledby="cta-heading">
    <div class="finale-inner">
      <img src={`${CF}/brand-logo-nav-circle/w=120,h=120,fit=contain,format=png`} alt="Sunshine on a Ranney Day" class="finale-icon" width="60" height="60" loading="lazy" decoding="async" />
      <h2 id="cta-heading" data-reveal>A room can change a child's life.<br/><em>You</em> can make it happen.</h2>
      <p data-reveal data-delay="1">Your donation helps build dream spaces for children with special needs in the Atlanta area. Every gift makes a real difference for kids, families, and community.</p>
      <div class="finale-ctas" data-reveal data-delay="2">
        <a href="/donate/" class="btn btn-dark btn-lg">Donate Today</a>
        <a href="/partners/" class="btn btn-outline-dark">Become a Partner</a>
      </div>
      <p class="finale-note" data-reveal data-delay="3">All donations tax-deductible · 501(c)(3) · EIN 45-4773997</p>
    </div>
  </section>

</BaseLayout>

<style>
  /* ── LQIP blur-up placeholders ── */
  .mission-photo,
  .story-photo,
  .kid-thumb-photo,
  .process-photo,
  .rm-parallax {
    background-size: cover;
    background-position: center;
  }
  .lqip-img.lqip-pending {
    opacity: 0;
  }
  .lqip-img:not(.lqip-pending) {
    transition: opacity 0.4s ease;
  }

  /* ══════ HERO — Editorial ══════ */
  .hero {
    position: relative;
    background: var(--c-cream);
    contain: layout style;
    padding: clamp(40px, 8vh, 100px) 0 0;
    overflow: hidden;
  }

  .hero-inner {
    max-width: var(--max-width-wide);
    margin: 0 auto;
    padding: 0 var(--page-padding);
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: clamp(32px, 5vw, 80px);
    align-items: center;
    min-height: clamp(480px, 70vh, 720px);
  }

  /* ── Text column ── */
  .hero-text {
    /* h1 must be visible on first paint for LCP — animate children instead */
  }

  .hero-eyebrow {
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--c-text-light);
    margin-bottom: 28px;
    opacity: 0;
    animation: heroUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) 0.04s both;
  }

  .hero-headline {
    font-size: clamp(42px, 6.5vw, 88px);
    line-height: 1.04;
    letter-spacing: -0.035em;
    color: var(--c-dark);
    margin-bottom: 28px;
  }
  .hero-headline :global(em) {
    font-style: italic;
    background-image: linear-gradient(transparent 52%, var(--c-yellow-soft) 52%);
    background-repeat: no-repeat;
    background-size: 100% 100%;
    padding: 0 0.05em;
  }

  .hero-sub {
    font-size: clamp(16px, 1.3vw, 19px);
    line-height: 1.7;
    color: var(--c-text-muted);
    max-width: 480px;
    margin-bottom: 36px;
    opacity: 0;
    animation: heroUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) 0.1s both;
  }

  .hero-ctas {
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
    opacity: 0;
    animation: heroUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) 0.18s both;
  }
  /* pulseGlow animation defined in global.css */

  /* ── Photo column ── */
  .hero-photo-wrap {
    position: relative;
    width: 100%;
    aspect-ratio: 4 / 5;
    /* NO opacity:0 — hero image is LCP candidate, must paint on load */
    animation: heroPhotoIn 0.35s cubic-bezier(0.16, 1, 0.3, 1) both;
  }

  .hero-photo {
    display: block;
    position: relative;
    border-radius: 24px;
    overflow: hidden;
    aspect-ratio: 4 / 5;
    box-shadow: 0 32px 80px rgba(0, 0, 0, 0.12), 0 8px 24px rgba(0, 0, 0, 0.06);
    text-decoration: none;
    color: inherit;
  }
  .hero-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    /* --parallax-y is set by JS scroll handler; defaults to 0 */
    transform: translate3d(0, var(--parallax-y, 0px), 0);
    transition: transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .hero-photo:hover img {
    transform: scale(1.03) translate3d(0, var(--parallax-y, 0px), 0);
  }

  .hero-photo-name {
    position: absolute;
    bottom: 16px;
    left: 16px;
    padding: 8px 18px;
    background: rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(16px) saturate(1.4);
    -webkit-backdrop-filter: blur(16px) saturate(1.4);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 100px;
    font-size: 13px;
    font-weight: 600;
    color: #fff;
    letter-spacing: 0.01em;
    pointer-events: none;
  }

  /* Yellow accent rectangle behind photo */
  .hero-photo-accent {
    position: absolute;
    bottom: -16px;
    right: -16px;
    width: 50%;
    height: 40%;
    border-radius: 20px;
    background: var(--c-yellow);
    z-index: -1;
    opacity: 0;
    animation: accentIn 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.5s both;
  }



  /* ── Animations ── */
  @keyframes heroUp {
    from { opacity: 0; transform: translateY(28px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes heroPhotoIn {
    from { transform: translateY(12px) scale(0.98); }
    to { transform: translateY(0) scale(1); }
  }
  @keyframes accentIn {
    from { opacity: 0; transform: translate(12px, 12px); }
    to { opacity: 1; transform: translate(0, 0); }
  }

  /* ── Responsive ── */
  @media (max-width: 900px) {
    .hero { padding-top: clamp(24px, 5vh, 48px); }
    .hero-inner {
      grid-template-columns: 1fr;
      gap: 40px;
      min-height: auto;
    }
    .hero-headline {
      font-size: clamp(36px, 9vw, 56px);
    }
    .hero-photo-wrap {
      max-width: 400px;
      margin: 0 auto;
    }
    .hero-photo-accent {
      bottom: -12px;
      right: -12px;
    }
  }

  @media (max-width: 480px) {
    .hero-headline {
      font-size: clamp(32px, 8.5vw, 42px);
    }
    .hero-sub {
      font-size: 15px;
    }
    .hero-ctas .btn {
      padding: 12px 26px;
      font-size: 14px;
    }
    .hero-photo-wrap {
      max-width: 320px;
    }
    .hero-photo {
      border-radius: 18px;
    }

  }
  .btn-ghost-light {
    background: rgba(255,255,255,0.08);
    color: #fff;
    border: 1.5px solid rgba(255,255,255,0.2);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  .btn-ghost-light:hover {
    background: rgba(255,255,255,0.15);
    border-color: rgba(255,255,255,0.4);
    transform: translateY(-3px);
  }

  /* ── RIBBON ── */
  .ribbon { background: var(--c-dark-deep); padding: 56px 0; contain: layout style; min-height: 160px; }
  .ribbon-inner { max-width: var(--max-width); margin: 0 auto; padding: 0 var(--page-padding); display: flex; justify-content: center; align-items: center; gap: 48px; flex-wrap: wrap; }
  .ribbon-stat { text-align: center; min-width: 130px; }
  .ribbon-num { font-family: var(--font-display); font-size: clamp(36px, 5vw, 52px); font-weight: 700; color: var(--c-yellow); line-height: 1; }
  .ribbon-pct { font-size: 0.6em; }
  .ribbon-label { display: block; font-size: 12px; font-weight: 600; color: rgba(255,255,255,0.7); text-transform: uppercase; letter-spacing: 0.12em; margin-top: 8px; }
  .ribbon-divider { width: 1px; height: 40px; background: rgba(255,255,255,0.08); }
  @media (max-width: 600px) { .ribbon-inner { display: grid; grid-template-columns: 1fr 1fr; gap: 32px; justify-items: center; } .ribbon-divider { display: none; } .ribbon-stat { min-width: 0; } .ribbon-stat:last-of-type { grid-column: 1 / -1; } }

  /* ── MISSION ── */
  .mission { padding: 120px 0; contain: layout style; }
  .mission-inner { max-width: var(--max-width); margin: 0 auto; padding: 0 var(--page-padding); display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: center; }
  .mission h2 { font-size: clamp(28px, 3.5vw, 40px); margin-bottom: 28px; line-height: 1.25; }
  .mission h2 em { font-style: italic; }
  .mission-body { font-size: 17px; line-height: 1.8; margin-bottom: 16px; color: var(--c-text-muted); }
  .mission-body em { font-style: italic; color: var(--c-dark); }
  .mission-visual { position: relative; min-height: 480px; }
  .mission-photo { border-radius: 20px; overflow: hidden; box-shadow: 0 24px 64px rgba(0,0,0,0.1); }
  .mission-photo img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .mission-photo--main { position: absolute; top: 0; left: 0; width: 72%; height: 70%; z-index: 1; }
  .mission-photo--accent { position: absolute; bottom: 0; right: 0; width: 58%; height: 55%; z-index: 2; border: 5px solid var(--c-cream); }
  @media (max-width: 768px) { .mission { padding: 80px 0; } .mission-inner { grid-template-columns: 1fr; gap: 40px; } .mission-visual { min-height: 340px; } }

  /* ── STORY ── */
  .story { background: var(--c-cream); contain: layout style; padding: 80px 0; }
  .story-inner { max-width: var(--max-width); margin: 0 auto; padding: 0 var(--page-padding); }
  .story-split { display: grid; grid-template-columns: 1fr 1fr; border-radius: 20px; overflow: hidden; background: #fff; box-shadow: 0 4px 32px rgba(0,0,0,0.06), 0 1px 4px rgba(0,0,0,0.04); }
  .story-photo { position: relative; overflow: hidden; margin: 0; height: clamp(440px, 36vw, 560px); }
  .story-photo-img { width: 100%; height: 100%; object-fit: cover; object-position: center top; display: block; }
  .story-photo-tag { position: absolute; top: 24px; left: 24px; background: var(--c-yellow); color: var(--c-dark); font-size: 12px; font-weight: 700; padding: 6px 16px; border-radius: 50px; letter-spacing: 0.04em; z-index: 1; }
  .story-content { padding: 56px 60px; display: flex; flex-direction: column; justify-content: center; }
  .story-type { display: inline-block; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.12em; color: var(--c-accent); margin-bottom: 16px; }
  .story-content h2 { font-size: clamp(22px, 2.2vw, 28px); line-height: 1.35; margin-bottom: 20px; letter-spacing: -0.01em; }
  .story-content p { font-size: 15px; line-height: 1.8; margin-bottom: 32px; color: var(--c-text-muted); }
  .story-cta { align-self: flex-start; }
  @media (max-width: 768px) {
    .story { padding: 48px 0; }
    .story-split { grid-template-columns: 1fr; border-radius: 16px; }
    .story-photo { height: 300px; }
    .story-content { padding: 36px 28px 40px; }
  }

  /* ── KIDS STRIP ── */
  .kids-strip { padding: 100px 0 80px; overflow: hidden; contain: layout style; }
  .kids-strip-header { max-width: var(--max-width); margin: 0 auto 48px; padding: 0 var(--page-padding); display: flex; align-items: flex-end; justify-content: space-between; flex-wrap: wrap; gap: 16px; }
  .kids-strip-header h2 { margin: 0; }
  .kids-strip-header h2 em { font-style: italic; }
  .kids-strip-all { font-size: 15px; font-weight: 600; color: var(--c-accent); transition: letter-spacing 0.3s ease; white-space: nowrap; }
  .kids-strip-all:hover { letter-spacing: 0.02em; }
  .kids-scroll-wrap { position: relative; }
  .kids-scroll { display: flex; gap: 24px; padding: 0 var(--page-padding); overflow-x: auto; scroll-snap-type: x mandatory; -webkit-overflow-scrolling: touch; scrollbar-width: none; list-style: none; margin: 0; scroll-behavior: smooth; }
  .kids-scroll::-webkit-scrollbar { display: none; }
  .kid-thumb-item { flex-shrink: 0; width: 220px; scroll-snap-align: start; }
  .kid-thumb { display: block; text-align: center; text-decoration: none; color: inherit; }
  .kid-thumb-photo { width: 220px; height: 220px; border-radius: 20px; overflow: hidden; margin-bottom: 16px; background: var(--c-warm-gray); }
  .kid-thumb-photo img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.4s cubic-bezier(0.16,1,0.3,1); }
  .kid-thumb:hover .kid-thumb-photo img { transform: scale(1.06); }
  .kid-thumb-name { font-size: 17px; margin-bottom: 2px; }
  .kid-thumb p { font-size: 13px; color: var(--c-text-muted); }
  .kids-scroll-btn { position: absolute; top: 110px; transform: translateY(-50%); width: 44px; height: 44px; border-radius: 50%; background: var(--c-white); border: 1px solid var(--c-border); color: var(--c-dark); display: flex; align-items: center; justify-content: center; cursor: pointer; box-shadow: 0 2px 8px rgba(0,0,0,0.08); transition: all 0.25s ease; z-index: 2; opacity: 0; pointer-events: none; }
  .kids-scroll-btn.is-active { opacity: 1; pointer-events: auto; }
  .kids-scroll-btn:hover { background: var(--c-dark); color: var(--c-white); box-shadow: 0 4px 16px rgba(0,0,0,0.15); }
  .kids-scroll-btn--prev { left: 8px; }
  .kids-scroll-btn--next { right: 8px; }
  @media (max-width: 768px) { .kids-strip { padding: 64px 0 48px; } .kids-scroll-btn { display: none; } }
  @media (hover: none) and (pointer: coarse) { .kids-scroll-btn { display: none; } }

  /* ── PROCESS ── */
  .process { padding: 120px 0; background: var(--c-dark-deep); contain: layout style; }
  .process-inner { max-width: var(--max-width-wide); margin: 0 auto; padding: 0 var(--page-padding); }
  .process-header { text-align: center; margin-bottom: 80px; }
  .process-header .section-label { justify-content: center; color: var(--c-yellow); }
  .process-header h2 { color: #fff; }
  .process-header h2 em { font-style: italic; color: var(--c-yellow); }

  /* Two-column split */
  .process-split { display: grid; grid-template-columns: 1fr 1.15fr; gap: 64px; align-items: start; }

  /* Photo column */
  .process-photo { border-radius: 24px; overflow: hidden; position: sticky; top: 100px; }
  .process-photo img { width: 100%; height: auto; display: block; object-fit: cover; aspect-ratio: 3 / 4; object-position: 60% center; }

  /* Steps column */
  .process-content { padding-top: 8px; }
  .process-timeline { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 0; }

  /* Each step */
  .process-step { padding: 32px 0; border-bottom: 1px solid rgba(255,255,255,0.06); }
  .process-step:first-child { padding-top: 0; }
  .process-step:last-child { border-bottom: none; }
  .process-num { display: inline-block; font-family: var(--font-display); font-size: 13px; font-weight: 700; letter-spacing: 0.1em; color: var(--c-dark); background: var(--c-yellow); padding: 4px 12px; border-radius: 6px; margin-bottom: 16px; }
  .process-step h3 { font-size: 22px; color: #fff; margin-bottom: 8px; line-height: 1.3; }
  .process-step p { font-size: 15px; line-height: 1.75; color: rgba(255,255,255,0.5); margin: 0; }

  /* Highlight step (The Big Reveal) */
  .process-step--highlight { background: rgba(255,218,36,0.04); margin: 0 -24px; padding: 32px 24px; border-radius: 16px; border-bottom: none; }
  .process-badge { display: inline-block; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; color: var(--c-yellow); margin-bottom: 10px; }

  /* CTA */
  .process-cta { margin-top: 40px; }

  @media (max-width: 768px) {
    .process { padding: 80px 0; }
    .process-header { margin-bottom: 48px; }
    .process-split { grid-template-columns: 1fr; gap: 40px; }
    .process-photo { position: static; border-radius: 20px; }
    .process-photo img { aspect-ratio: 16 / 10; }
    .process-cta { text-align: center; }
  }

  /* ══ ROOMS — Editorial magazine layout ══ */
  .rooms { padding: 140px 0 160px; background: var(--c-cream); contain: layout style; }
  .rooms-inner { max-width: var(--max-width-wide); margin: 0 auto; padding: 0 var(--page-padding); }
  .rooms-header { max-width: 600px; margin-bottom: 80px; }
  .rooms-header h2 { line-height: 1.18; letter-spacing: -0.02em; }
  .rooms-header h2 em { font-style: italic; }

  /* ── Shared editorial elements ── */
  .rm-num {
    display: block;
    font-family: var(--font-display); font-style: italic;
    font-size: clamp(72px, 10vw, 120px); font-weight: 400;
    line-height: 0.85; color: #E0DDD6;
    transition: color 0.6s ease;
    user-select: none;
  }
  .rm-rule {
    width: 48px; height: 3px;
    background: var(--c-yellow);
    margin: 20px 0 24px;
    border-radius: 2px;
    transition: width 0.6s cubic-bezier(0.16,1,0.3,1);
  }
  .rm-cta {
    display: inline-flex; align-items: center; gap: 8px;
    font-size: 13px; font-weight: 700; letter-spacing: 0.08em;
    text-transform: uppercase; color: var(--c-dark);
    transition: gap 0.45s cubic-bezier(0.16,1,0.3,1);
  }
  .rm-cta svg {
    transition: transform 0.45s cubic-bezier(0.16,1,0.3,1);
    will-change: transform;
  }

  /* ── Parallax image container ── */
  .rm-parallax {
    overflow: hidden; border-radius: 20px;
    background-size: cover; background-position: center;
    contain: content;
  }
  .rm-parallax img {
    width: 100%; height: 100%; object-fit: cover;
    transform: scale(1.06);
    transition: transform 1s cubic-bezier(0.16,1,0.3,1);
  }
  @media (min-width: 768px) {
    .rm-parallax img { will-change: transform; }
  }

  /* ════════════════════════════════════
     TRIO CARDS — three-column grid
     ════════════════════════════════════ */
  .rm-trio {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 40px;
  }
  .rm-card-link {
    display: block; text-decoration: none; color: inherit;
  }
  .rm-card-img {
    aspect-ratio: 4 / 5; border-radius: 20px;
    margin-bottom: 28px;
  }
  .rm-card-link:hover .rm-card-img img { transform: scale(1.04); }
  .rm-card-link:hover .rm-num { color: var(--c-yellow); }
  .rm-card-link:hover .rm-rule { width: 88px; }
  .rm-card-link:hover .rm-cta { gap: 14px; }
  .rm-card-link:hover .rm-cta svg { transform: translateX(4px); }

  .rm-card-text .rm-num { font-size: clamp(56px, 7vw, 88px); margin-bottom: -6px; }
  .rm-card-text h3 {
    font-family: var(--font-display);
    font-size: clamp(24px, 2.8vw, 34px);
    font-weight: 700; line-height: 1.15;
    letter-spacing: -0.02em;
  }
  .rm-card-text p {
    font-size: 15px; line-height: 1.7;
    color: var(--c-text-muted);
    margin-bottom: 28px;
  }

  /* ── Focus states ── */
  .rm-spread-link:focus-visible,
  .rm-card-link:focus-visible {
    outline: 3px solid var(--c-yellow); outline-offset: 8px;
    border-radius: 24px;
  }

  /* ════════ Responsive ════════ */
  @media (max-width: 900px) {
    .rooms { padding: 100px 0 120px; }
    .rm-trio { gap: 32px; }
    .rm-num { font-size: clamp(56px, 8vw, 80px); }
    .rm-card-img { aspect-ratio: 4 / 5; }
    .rm-card-text .rm-num { font-size: clamp(40px, 6vw, 56px); }
  }
  @media (max-width: 600px) {
    .rooms { padding: 56px 0 64px; }
    .rooms-header { margin-bottom: 32px; }
    .rooms-header h2 { font-size: 24px; }

    /* Numbers: small accent, not a hero element */
    .rm-num { font-size: 14px; font-style: normal; font-weight: 700;
      letter-spacing: 0.1em; color: var(--c-yellow);
      margin-bottom: 8px;
    }

    /* Rule: thinner on mobile */
    .rm-rule { margin: 12px 0 14px; width: 32px; height: 2px; }

    /* Trio cards — horizontal scroll carousel on mobile */
    .rm-trio {
      display: flex; gap: 16px;
      overflow-x: auto; scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch; scrollbar-width: none;
      margin: 0 calc(var(--page-padding) * -1);
      padding: 0 var(--page-padding);
    }
    .rm-trio::-webkit-scrollbar { display: none; }
    /* Right padding so last card doesn't hit the edge */
    .rm-trio::after {
      content: ''; flex: 0 0 var(--page-padding);
    }

    .rm-card {
      flex: 0 0 78%; min-width: 0; scroll-snap-align: start;
    }
    .rm-card-link {
      display: block; position: relative;
      border-radius: 16px; overflow: hidden;
      height: 100%;
    }
    .rm-card-img {
      aspect-ratio: 4 / 5; max-height: 420px;
      border-radius: 0; margin-bottom: 0;
      height: 100%;
    }
    .rm-card-text {
      position: absolute; bottom: 0; left: 0; right: 0;
      padding: 24px;
      background: linear-gradient(to top, rgba(0,0,0,0.78) 0%, rgba(0,0,0,0.35) 60%, transparent 100%);
    }
    .rm-card-text .rm-num { font-size: 14px; color: var(--c-yellow); margin-bottom: 6px; }
    .rm-card-text h3 { font-size: 22px; color: #fff; font-family: var(--font-display); }
    .rm-card-text .rm-rule { background: var(--c-yellow); width: 28px; margin: 10px 0 12px; }
    .rm-card-text p { font-size: 13px; line-height: 1.6; color: rgba(255,255,255,0.85); margin-bottom: 16px; }
    .rm-card-text .rm-cta { color: #fff; font-size: 12px; }

    .rm-parallax { border-radius: 0; }
    .rm-cta { padding: 4px 0; }
  }
  @media (prefers-reduced-motion: reduce) {
    .rm-parallax img { transition: none; }
  }

  /* ── TRUST ── */
  .trust { padding: 120px 0; background: var(--c-dark-deep); color: #fff; contain: layout style; }
  .trust-inner { max-width: var(--max-width); margin: 0 auto; padding: 0 var(--page-padding); }
  .trust-header { text-align: center; margin-bottom: 64px; }
  .trust-header .section-label { justify-content: center; }
  .trust-header h2 { color: #fff; font-size: clamp(28px, 4vw, 44px); margin-bottom: 16px; }
  .trust-header h2 em { font-style: italic; color: var(--c-yellow); }
  .trust-subtitle { font-size: 17px; color: rgba(255,255,255,0.5); max-width: 560px; margin: 0 auto; line-height: 1.7; }
  .trust-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; margin-bottom: 48px; }
  .trust-card { background: rgba(255,255,255,0.04); border: 1px solid rgba(255,255,255,0.06); border-radius: 20px; padding: 36px; transition: all 0.4s ease; }
  .trust-card:hover { background: rgba(255,255,255,0.07); transform: translateY(-4px); }
  .trust-card-top { margin-bottom: 16px; }
  .trust-big { display: block; font-family: var(--font-display); font-size: 40px; font-weight: 700; color: var(--c-yellow); line-height: 1; margin-bottom: 8px; }
  .trust-big--icon { font-family: inherit; font-size: 0; line-height: 0; }
  .trust-card-label { font-size: 13px; font-weight: 600; color: rgba(255,255,255,0.5); text-transform: uppercase; letter-spacing: 0.08em; }
  .trust-card p { font-size: 15px; color: rgba(255,255,255,0.6); line-height: 1.65; }
  .trust-cta { text-align: center; }
  @media (max-width: 768px) { .trust { padding: 80px 0; } .trust-grid { grid-template-columns: 1fr; } }


  /* ── FINALE ── */
  .finale { padding: 120px 0; background: var(--c-yellow); text-align: center; position: relative; overflow: hidden; contain: layout style; }
  .finale-inner { max-width: 640px; margin: 0 auto; padding: 0 var(--page-padding); position: relative; z-index: 2; }
  .finale-icon { width: clamp(72px, 9vw, 100px); height: auto; margin: 0 auto clamp(20px, 3vw, 28px); display: block; }
  .finale h2 { font-size: clamp(28px, 4vw, 40px); line-height: 1.25; margin-bottom: 20px; }
  .finale h2 em { font-style: italic; }
  .finale p { font-size: 17px; color: rgba(45,46,51,0.82); line-height: 1.7; margin-bottom: 36px; }
  .finale-ctas { display: flex; gap: 14px; justify-content: center; flex-wrap: wrap; margin-bottom: 24px; }
  .finale-note { font-size: 13px; color: rgba(45,46,51,0.72); margin-bottom: 0; }
  .btn-outline-dark { background: transparent; color: var(--c-dark); border: 2px solid var(--c-dark); }
  .btn-outline-dark:hover { background: var(--c-dark); color: var(--c-yellow); transform: translateY(-3px); }
  .btn-lg { font-size: 17px; padding: 18px 44px; }
  @media (max-width: 768px) { .finale { padding: 80px 0; } .finale-ctas .btn { flex: 1; justify-content: center; min-width: 160px; } }

  /* ── SCROLL REVEALS ── */
  [data-reveal] { opacity: 0; transform: translateY(32px); transition: opacity 0.7s cubic-bezier(0.16,1,0.3,1), transform 0.7s cubic-bezier(0.16,1,0.3,1); }
  [data-reveal].is-visible { opacity: 1; transform: translateY(0); }
  [data-delay="1"] { transition-delay: 0.1s; }
  [data-delay="2"] { transition-delay: 0.2s; }
  [data-delay="3"] { transition-delay: 0.3s; }
  [data-delay="4"] { transition-delay: 0.4s; }

  @media (prefers-reduced-motion: reduce) {
    [data-reveal] { opacity: 1; transform: none; transition: none; }
    .btn-hero-glow { animation: none; }

    .hero-headline, .hero-photo-wrap { transform: none; animation: none; }
    .hero-eyebrow, .hero-sub, .hero-ctas, .hero-photo-accent { opacity: 1; transform: none; animation: none; }
    .lqip-img:not(.lqip-pending) { transition: none; }
    .lqip-img.lqip-pending { opacity: initial; }
  }
</style>


<!-- Hero-specific no-JS fallback (data-reveal handled by ScrollReveal in BaseLayout) -->
<noscript>
  <style>
    .hero-headline, .hero-photo-wrap { transform: none !important; animation: none !important; }
    .hero-eyebrow, .hero-sub, .hero-ctas, .hero-photo-accent { opacity: 1 !important; animation: none !important; transform: none !important; }
  </style>
</noscript>

<script>
  /* ── Home page: hero photo parallax ──
   * Subtle vertical parallax on the hero photo for depth.
   * Uses astro:page-load for View Transition compatibility. */

  let _scrollHandler: (() => void) | null = null;

  function cleanup() {
    if (_scrollHandler) window.removeEventListener('scroll', _scrollHandler);
    _scrollHandler = null;
  }

  function initHomePage() {
    const hero = document.querySelector('.hero') as HTMLElement;
    if (!hero) return;

    cleanup();

    if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
      const photo = hero.querySelector('.hero-photo img') as HTMLElement;
      if (photo) {
        const heroH = hero.offsetHeight || window.innerHeight;
        let ticking = false;
        _scrollHandler = function () {
          if (ticking) return;
          ticking = true;
          requestAnimationFrame(() => {
            const scrollY = window.scrollY;
            if (scrollY < heroH) {
              photo.style.setProperty('--parallax-y', `${scrollY * 0.06}px`);
            }
            ticking = false;
          });
        };
        window.addEventListener('scroll', _scrollHandler, { passive: true });
      }
    }
  }

  document.addEventListener('astro:page-load', initHomePage);
  document.addEventListener('astro:before-swap', cleanup);
</script>

<!-- Kids strip scroll arrows (desktop only) -->
<script>
  function initKidsScroll() {
    const scroll = document.querySelector('.kids-scroll') as HTMLElement | null;
    const prev = document.querySelector('.kids-scroll-btn--prev') as HTMLElement | null;
    const next = document.querySelector('.kids-scroll-btn--next') as HTMLElement | null;
    if (!scroll || !prev || !next) return;

    function updateArrows() {
      const atStart = scroll!.scrollLeft <= 8;
      const atEnd = scroll!.scrollLeft + scroll!.clientWidth >= scroll!.scrollWidth - 8;
      prev!.classList.toggle('is-active', !atStart);
      next!.classList.toggle('is-active', !atEnd);
    }

    prev.addEventListener('click', () => { scroll.scrollBy({ left: -488, behavior: 'smooth' }); });
    next.addEventListener('click', () => { scroll.scrollBy({ left: 488, behavior: 'smooth' }); });
    scroll.addEventListener('scroll', updateArrows, { passive: true });
    updateArrows();
  }
  document.addEventListener('astro:page-load', initKidsScroll);
</script>

<!-- Room images: scroll-driven parallax (desktop only) -->
<script>
  let _rmObserver: IntersectionObserver | null = null;
  let _rmScroll: (() => void) | null = null;

  function rmCleanup() {
    _rmObserver?.disconnect(); _rmObserver = null;
    if (_rmScroll) { window.removeEventListener('scroll', _rmScroll); _rmScroll = null; }
  }

  function initRoomParallax() {
    rmCleanup();
    // Skip on mobile (no visual payoff, saves battery) and reduced-motion
    if (window.innerWidth < 768) return;
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

    const imgs = document.querySelectorAll<HTMLElement>('.rm-parallax img');
    if (!imgs.length) return;

    const visible = new Set<HTMLElement>();

    _rmObserver = new IntersectionObserver(entries => {
      entries.forEach(e => {
        if (e.isIntersecting) visible.add(e.target as HTMLElement);
        else visible.delete(e.target as HTMLElement);
      });
    }, { rootMargin: '60px 0px' });

    imgs.forEach(img => _rmObserver!.observe(img));

    let ticking = false;
    function onScroll() {
      if (ticking || !visible.size) return;
      ticking = true;
      requestAnimationFrame(() => {
        const vh = window.innerHeight;
        visible.forEach(img => {
          const rect = img.getBoundingClientRect();
          const progress = (rect.top + rect.height / 2 - vh / 2) / vh;
          img.style.transform = `scale(1.06) translateY(${progress * -16}px)`;
        });
        ticking = false;
      });
    }

    _rmScroll = onScroll;
    window.addEventListener('scroll', onScroll, { passive: true });
    onScroll(); // initial position
  }

  document.addEventListener('astro:page-load', initRoomParallax);
  document.addEventListener('astro:before-swap', rmCleanup);
</script>

<!-- Shared element morph: kid thumbnail → profile hero -->
<script>
  function initHomeKidTransitions() {
    document.querySelectorAll<HTMLAnchorElement>('.kid-thumb').forEach(thumb => {
      thumb.addEventListener('click', () => {
        const img = thumb.querySelector('img') as HTMLElement | null;
        const name = thumb.querySelector('.kid-thumb-name') as HTMLElement | null;
        if (img) img.style.viewTransitionName = 'kid-hero';
        if (name) name.style.viewTransitionName = 'kid-name';
      });
    });
  }
  document.addEventListener('astro:page-load', initHomeKidTransitions);
</script>
